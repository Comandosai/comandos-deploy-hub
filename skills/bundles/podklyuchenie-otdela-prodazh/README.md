# Подключение отдела продаж

Этот навык нужен для подключения уже существующей клиентской инфраструктуры.

Что делает:
- делает `preflight` по `n8n` и проекту;
- находит `Supabase`;
- находит `n8n`;
- загружает workflow отдела продаж;
- загружает внутренний CRM-operator workflow, если в пакете используется AmoCRM;
- загружает отдельный тестовый workflow для проверки базы;
- при первом разворачивании активирует только `06_WF_Test` и `Уведомления об ошибках в N8N`, а боевые workflow оставляет выключенными;
- создает и привязывает соединения `Supabase`, `Postgres`, `MCP`;
- при необходимости докачивает обязательные custom node packages и credential providers;
- при необходимости создает соединения `OpenRouter`, `OpenAI`, `Telegram`;
- выполняет первичную проверку работоспособности.
- не завершает этап, пока editor `n8n` не открыт без `Connection lost`, а свежие логи `n8n` не подтверждают, что битые refs ушли;
- после каждого этапа сам предлагает следующий конкретный шаг и выдает готовый копируемый блок.

## Когда использовать

Используйте этот навык, когда:
- база знаний уже загружена;
- у клиента уже есть или почти готова инфраструктура;
- нужно быстро связать `Supabase`, `n8n`, workflow и `MCP`.

## Откуда брать входные данные

По умолчанию навык должен сначала искать в корне проекта файл `DANNYE_DLYA_RAZVERTYVANIYA.md`.

Если файл найден:
- читать оттуда доступы, ключи, домены и пароли;
- не спрашивать повторно заполненные поля.

Если файла нет:
- спрашивать у пользователя только недостающие поля;
- или предложить создать шаблон этого файла.

## Что потребуется

Обязательно:
- доступ к `Supabase` или к серверу, где он стоит;
- доступ к `n8n` или к серверу, где он стоит;
- `x-license-key` для кастомного `MCP`.

Опционально:
- `OpenRouter API key`;
- `OpenAI API key`;
- `Telegram bot token`.

## Важно про Telegram credentials

Для всех credentials типа `telegramApi` в этом проекте `baseUrl` должен быть `null`.

Навык должен:
- не валидировать `baseUrl` как обязательное поле для `telegramApi`;
- не подставлять `https://api.telegram.org` вручную;
- считать `baseUrl = null` нормальным runtime-совместимым состоянием;
- после создания или обновления Telegram credentials отдельно проверить, что `baseUrl` действительно `null`.

## Важно про `MCP`

Для кастомного `MCP` поле `x-license-key` обязательно.

Если пользователь не дал `x-license-key`, навык должен остановиться на настройке `MCP` и запросить этот ключ.

Для `MCP AmoCRM` ручной шаблон headers по умолчанию не нужен.
Если в `DANNYE_DLYA_RAZVERTYVANIYA.md` уже заполнен `x_license_key`, навык должен сам создать credential `MCP AmoCRM` и не просить отдельный ручной блок headers или credential.

Для `MCP Postgres` одного `x_license_key` недостаточно. Нужны все параметры базы и полный набор headers.

## Важно про credentials в n8n 2.x

Навык должен:
- определять текущий `project_id` перед импортом credentials;
- импортировать credentials через `n8n import:credentials --project <current_project_id>`;
- считать `credential.id` валидным, если он непустой и принимается текущим n8n runtime; UUID-формат не требовать;
- использовать OpenAI credential type `openAiApi`;
- не использовать legacy type `openaiApi`;
- для `HTTP Multiple Headers Auth` использовать `headers.values`, а не прямой массив `headers`;
- принимать UI-created credential как нормальный формат, если он проходит runtime и корректно привязан к workflow;
- не считать `null` в non-secret полях автоматической ошибкой, если publish/activate и runtime execution проходят.

Для Supabase credential обязательно использовать `supabase_service_role_key`.
Anon key нельзя использовать вместо service role key.

Для повседневной работы использовать короткий reference:
- `references/credential-binding-lite.md`

Тяжёлые reference-файлы:
- `references/audit-pereprivyazki.md`
- `references/runtime-readiness.md`

нужно читать только на этапах `rebind / publish / activate / final runtime check`, а не тащить в контекст всегда.

## Важно про реальный сценарий

Этот навык должен по умолчанию делать четыре обязательные фазы:
- `Preflight`;
- `Secret discovery`;
- `Workflow import + rebind audit`;
- `Runtime readiness`.

По умолчанию навык должен:
- использовать интерактивную SSH-сессию для длинных операций;
- писать SQL-аудит по `n8n` через `nodes::jsonb`;
- после импорта workflow проверять unresolved credentials и project sharing;
- после импорта workflow обязательно делать credential rebind для всех refs, где есть `name`, но отсутствует `id`;
- если после rebind остался хотя бы один refs без `id`, не публиковать, не активировать и не считать этап завершенным;
- после первого импорта включать `06_WF_Test`;
- после первого импорта включать `Уведомления об ошибках в N8N` с `telegram_error_bot_token`;
- после импорта найти актуальный ID workflow `Уведомления об ошибках в N8N` и прописать его в `settings.errorWorkflow` всех workflow пакета, кроме самого workflow ошибок;
- не активировать боевые workflow `01-05` и внутренний `07_WF_CRM_Operator` до отдельной команды на боевой запуск отдела продаж;
- проверять и при необходимости докачивать:
  - `@comandosai/n8n-nodes-amo-crm`;
  - `@comandosai/n8n-nodes-doc-extract`;
  - provider для `comandosLsiKeysApi`.

## Как агент должен вести пользователя

После каждого этапа агент должен:
- сам понять, что уже сделано;
- не просить повторять шаги без причины;
- предлагать только один следующий шаг;
- выдавать этот шаг в готовом `text`-блоке;
- если после связки инфраструктуры уже все готово, сразу предлагать Telegram-тестирование.

## Команда 1. Скачать и установить навык

Скопируйте и вставьте агенту:

```text
Установи навык одной командой:

`curl -sSL https://raw.githubusercontent.com/Comandosai/comandos-deploy-hub/main/tools/skills.sh | bash -s -- install podklyuchenie-otdela-prodazh --client codex`

Если работаешь в Claude, замени `codex` на `claude`.
Если работаешь в Gemini, замени `codex` на `gemini`.
Если работаешь в Antigravity, замени `codex` на `antigravity`.

После установки ничего не запускай автоматически.
Просто покажи, что навык установлен, и скажи, какой следующей командой запускать подключение инфраструктуры.
```

## Команда 2. Preflight инфраструктуры

Скопируйте и вставьте агенту:

```text
Используй навык `Подключение отдела продаж`.

Сначала выполни обязательный preflight:
- подключись к серверу через интерактивную SSH-сессию;
- найди `n8n`;
- определи `project_id`;
- проверь контейнеры и текущее состояние проекта;
- проверь установленные custom node packages;
- проверь зарегистрированные credential types;
- отдельно проверь:
  - `@comandosai/n8n-nodes-amo-crm`
  - `@comandosai/n8n-nodes-doc-extract`
  - `comandosLsiKeysApi`

Если чего-то не хватает для запуска workflow, докачай это до этапа импорта или явно скажи, что именно отсутствует.
После этого покажи короткий preflight-отчет.
```

## Команда 3. Найти `Supabase` и `n8n`

Скопируйте и вставьте агенту:

```text
Используй навык `Подключение отдела продаж`.

Сначала проверь инфраструктуру клиента:
- найди `Supabase`;
- найди `n8n`;
- определи, находятся ли они на одном сервере или на разных;
- если что-то не найдено автоматически, запроси у меня только недостающие доступы.

Также проверь:
- используется ли стандартная схема `public` или другая;
- есть ли уже созданные соединения;
- есть ли уже загруженные workflow.

После этого коротко покажи, что ты нашел.
```

## Команда 4. Secret discovery и создание соединений

Скопируйте и вставьте агенту:

```text
Теперь сначала найди секреты автоматически, а потом настрой соединения.

Нужно:
- проверить `.env`, docker env и рабочие каталоги;
- разделить результат на:
  - `нашел и создал`
  - `не нашел, запросить у пользователя`

- создать или обновить соединение `Supabase`;
- создать или обновить соединение `Postgres`;
- создать или обновить соединение `MCP Postgres`.
- создать или обновить соединение `MCP AmoCRM`.

Для `MCP Postgres` используй:
- credential type: `HTTP Multiple Headers Auth`
- endpoint: `https://postgres.mcp.comandos.ai/`

Headers:
- `db_host`
- `db_port`
- `db_name`
- `db_user`
- `db_password`
- `db_schema`
- `x-license-key`
- `tenant_id` допустим как дополнительный header, если его создал UI или он реально нужен текущему runtime

Используй только эти точные имена заголовков.
Не подменяй их на `DB_HOST`, `database_host`, `x_license_key`, `db-user` или другие варианты.
Поле `x_license_key` допустимо только в файле данных, но в credential header должно быть именно `x-license-key`.
В credential JSON используй структуру `headers.values`, а не прямой массив `headers`.

Для `MCP AmoCRM` используй:
- credential type: `HTTP Multiple Headers Auth`
- endpoint: `https://amocrm.mcp.comandos.ai`
- header: `x-license-key`

Для Supabase используй:
- host/API URL;
- `supabase_service_role_key`.

Не используй anon key вместо service role key.

Если каких-то параметров не хватает, запроси у меня только недостающее.
Если схема не указана, спроси, используется ли `public`.

Отдельно проверь:
- не нужен ли Supavisor-style username для внешнего `Supabase/Postgres`;
- что endpoint для `MCP Postgres` живет в node, а в credential идут только headers.
```

## Команда 5. Загрузить workflow

Скопируйте и вставьте агенту:

```text
Теперь загрузи workflow отдела продаж в `n8n`.

Нужно загрузить:
- прием входящих сообщений;
- главный оркестратор;
- квалификацию;
- консультацию;
- передачу человеку;
- CRM-оператор `07_WF_CRM_Operator`, если он есть в пакете;
- тестовый workflow `06_WF_Test`;
- уведомления об ошибках;
- отдельный тестовый workflow `06_WF_Test` для проверки базы.

Не плодить дубликаты, если эти workflow уже есть.
После загрузки покажи, какие workflow были импортированы или обновлены.
```

## Команда 6. Аудит и перепривязка соединений

Скопируйте и вставьте агенту:

```text
Теперь после импорта workflow сделай обязательный аудит и перепривязку соединений.

Нужно:
- проверить unresolved credentials через SQL-аудит;
- использовать `nodes::jsonb`;
- отдельно проверить refs, у которых есть `name`, но отсутствует `id`;
- выполнить обязательный rebind `(type + name) -> id` для таких refs;
- повторить SQL-аудит и убедиться, что refs с `name` без `id` больше нет;
- если после rebind такие refs остались, остановить этап с ошибкой и не переходить к activation/publish;
- различать:
  - credential отсутствует;
  - credential существует, но не расшарен в проект;
- проверить `shared_credentials` для текущего `project_id`;
- найти актуальный ID workflow `Уведомления об ошибках в N8N`;
- прописать этот ID в `settings.errorWorkflow` всех workflow пакета, кроме самого workflow ошибок;
- проверить, что падения `01-07` будут уходить в workflow ошибок, если `07_WF_CRM_Operator` есть в пакете;
- подставить `Supabase`;
- подставить `Postgres`;
- подставить `MCP`;
- отдельно проверить и при необходимости создать `MCP Postgres`;
- отдельно проверить и при необходимости создать `MCP AmoCRM`;
- если в файле данных уже есть `x_license_key`, создать `MCP AmoCRM` автоматически без отдельного запроса headers;
- если даны ключи, подставить `OpenRouter`, `OpenAI`, `Telegram`;
- для Telegram создать отдельные credentials `Telegram Sales Bot` и `Telegram Error Bot`, если есть оба токена;
- не использовать битые или пустые credentials;
- проверить, что у workflow не осталось незаполненных соединений.

После завершения покажи, что было привязано.
```

## Команда 7. Настроить тестового агента

Скопируйте и вставьте агенту:

```text
Настрой отдельный workflow `06_WF_Test`.

Он должен:
- использовать общий тестовый prompt;
- работать через `MCP`;
- использовать `knowledge_rag` как основной источник;
- использовать `products_live` для точных товарных данных;
- не выдумывать ответы;
- прямо говорить, если данных в базе не хватает.
```

## Команда 8. Runtime readiness и финальная проверка

Скопируйте и вставьте агенту:

```text
Проверь runtime readiness и итоговую работоспособность.

Нужно проверить:
- доступ к `Supabase`;
- доступ к `Postgres`;
- доступ к `MCP`;
- доступ к `MCP AmoCRM`, если он используется;
- наличие `x-license-key`;
- наличие обязательных node packages;
- наличие обязательных credential types;
- загрузку workflow;
- отсутствие пустых соединений;
- открыть editor `n8n` после полного refresh страницы и убедиться, что нет постоянного `Connection lost`;
- проверить свежие логи `n8n` и убедиться, что там нет:
  - `Found credential with no ID`
  - `Workflow activation failed validation`
  - `User attempted to access a workflow without permissions`
- хотя бы один тестовый запуск.
- что SQL-аудит refs с `credential.name` без `credential.id` вернул ноль строк;
- что активны только `06_WF_Test` и `Уведомления об ошибках в N8N`;
- что боевые workflow `01-05` и внутренний `07_WF_CRM_Operator` выключены до отдельной команды.
- что у `01-07` указан актуальный `settings.errorWorkflow` на workflow ошибок, если `07_WF_CRM_Operator` есть в пакете.

После этого дай краткий итоговый отчет:
- что найдено;
- что подключено;
- какие workflow загружены;
- что было докачано;
- что осталось сделать вручную, если осталось.
```
