# Comandos policy note:
# runtime docker-compose для Supabase берется из официального репозитория supabase/supabase (docker/).
# setup.sh автоматически подтягивает этот стек и создаёт override: docker-compose.comandos.yml
