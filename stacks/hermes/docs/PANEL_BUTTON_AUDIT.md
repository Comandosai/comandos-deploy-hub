# Аудит кнопок COMANDOS Hermes Workspace

Дата обновления: 2026-06-07

## Цель

Довести панель COMANDOS Hermes Workspace до состояния, где каждое видимое действие проверено руками как в реальном продукте: человек нажимает кнопку, смотрит, что изменилось в интерфейсе, проверяет network/browser console и свежий серверный лог, выявляет ошибку, исправляет причину и повторяет проверку для каждой кнопки, найденной на панели.

Каждая кнопка должна в итоге:

- работает;
- или честно отключено с понятным русским объяснением;
- или скрыто до готовности функции.

На эту задачу дано общее разрешение владельца: выполнять связанные с аудитом команды, проверки, клики и исправления без отдельных уточнений на каждый шаг. Секреты, `.env`, сессии, логи с личными данными и ключи при этом не печатаются и не коммитятся.

## Статусы

- `OK` — проверено, работает.
- `FIXED` — была ошибка, исправлено и перепроверено.
- `DISABLED` — функция недоступна, но интерфейс объясняет это по-русски.
- `HIDDEN` — функция убрана до готовности.
- `PARTIAL` — прогон нашёл дефект в одной части сценария; исправление описано рядом.
- `TODO` — ещё нужен живой VPS/gateway или отдельная коробочная проверка.

## Среда проверки

- Локальная панель: `http://127.0.0.1:33030`
- Живая QA-панель: `https://qa.194.113.38.109.nip.io`
- Живая основная панель: `https://194.113.38.109.nip.io`
- Ветка: `codex/product-button-audit`
- Проверка маршрутов: Playwright Chromium, чистые контексты, `claude-onboarding-complete=true`
- Локальный Hermes gateway: не поднят. Разделы, зависящие от gateway, проверялись на честное русское disabled-состояние.
- Живой Hermes gateway на `clawd`: доступен через `127.0.0.1:8642`, workspace через `127.0.0.1:3030`, сервисы `comandos-workspace`, `hermes-gateway`, `comandos-telegram` активны как user services.

## Исправленные дефекты

| Раздел                    | Проблема                                                                                                                                                                                                                                         | Исправление                                                                                                                                                                                                                                     | Перепроверка                                                                                                                                                                                                                        | Статус |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Задачи                    | У карточки задачи не было реального удаления                                                                                                                                                                                                     | Добавлен backend DELETE для задач/kanban и кнопка `Удалить` в форме редактирования                                                                                                                                                              | Создание, редактирование, удаление временной задачи через UI и API                                                                                                                                                                  | FIXED  |
| Задачи                    | Кнопка `Сохранить` меняла название/описание/колонку, но теряла `priority`, `tags` и `due_date` при работе через общий kanban backend                                                                                                             | Метаданные задачи сохраняются в скрытом служебном блоке `spec`, при чтении вынимаются обратно и не показываются в описании                                                                                                                      | QA `.53`: задача создана с `high`, тегами и сроком; после `Сохранить` стала `low`, получила новые теги/срок; затем удалена через UI                                                                                                 | FIXED  |
| Задачи / доска            | Широкий live-скан `.130` нашёл пять настоящих кнопок `Добавить в ...` в заголовках колонок `/tasks` размером `18x18`; действие работало, но зона клика была слишком маленькой для коробочной панели | Workspace поднят до `2.3.0-comandos.131`; кнопки добавления в колонку получили стабильную зону `32x32`, явный русский `aria-label` и сохранили прежнее открытие формы создания задачи | QA и main `.131`: Playwright проверил все пять кнопок `Добавить в "Разобрать"`, `Готово к работе`, `В работе`, `Проверка`, `Стоп`; каждая `32x32`, клик открывает форму создания, browser console/page/HTTP 5xx и свежие user-журналы чистые | FIXED  |
| Задачи / задания           | Широкий live-скан `.133` нашёл маленькие верхние действия: `/tasks` `Показать готовые` `121x26`, `Обновить` `28x28`, `Новая задача` `120x28`, а `/jobs` `Обновить список` `28x28`; такие кнопки работали, но были меньше нормальной зоны клика | Workspace поднят до `2.3.0-comandos.134`; верхние действия `/tasks` и `/jobs` получили высоту/зону `32px`, а icon-only действия карточек заданий получили стабильные `32x32` и явные русские `aria-label` | QA и main `.134`: Playwright кликнул `/tasks` `Показать готовые`/`Скрыть готовые`/`Обновить`/`Новая задача` и `/jobs` `Обновить список`; размеры стали `32px`, форма задачи открывается, карточек заданий в текущих данных не было видно; browser console/page/HTTP 5xx и свежие user-журналы чистые | FIXED  |
| Профили                   | Базовый профиль можно было пытаться переименовать                                                                                                                                                                                                | Для `default` переименование отключено, tooltip объясняет причину                                                                                                                                                                               | `default` показывает disabled `Переименовать`; временный профиль создаётся/переименовывается/активируется/удаляется                                                                                                                 | FIXED  |
| Импорт из ClawBot         | Пользователь не понимал, что именно запускается                                                                                                                                                                                                  | Проверен сценарий: кнопка открывает чат и вставляет промпт мигратора без копирования секретов                                                                                                                                                   | `/profiles` -> `Импорт из ClawBot` -> `/chat/main`, промпт виден                                                                                                                                                                    | OK     |
| OpenAI Codex              | Показывался ложный OAuth-сценарий                                                                                                                                                                                                                | Codex описан как `CLI-вход`; OAuth/API Key для него отключены                                                                                                                                                                                   | `/settings` и `/settings/providers`: нет `Start OAuth`, нет `OAuth device flow not supported`                                                                                                                                       | FIXED  |
| OpenAI Codex              | В CLI-сценарии не было явной кнопки перехода к проверке, а часть текста оставалась на английском                                                                                                                                                 | Добавлена кнопка `Перейти к проверке`; видимые подписи `API Key`, `API keys are stored locally`, `Show/Hide manual config snippet` русифицированы                                                                                               | `clawd` обновлён до `.17`; Playwright прошёл `Добавить провайдера → OpenAI Codex → CLI-вход → Перейти к проверке` без console/network ошибок                                                                                        | FIXED  |
| Лицензия                  | Ошибки вроде `License key required` показывались по-английски                                                                                                                                                                                    | Ошибки лицензии нормализованы в русские сообщения                                                                                                                                                                                               | Mock-проверка экрана лицензии: до ввода ошибки нет, после пустой активации русское сообщение                                                                                                                                        | FIXED  |
| Вход                      | Неверный пароль на экране входа показывался как `Invalid password`, а обычная пользовательская ошибка уходила в browser/network как HTTP 401                                                                                                      | Workspace поднят до `2.3.0-comandos.69`; `/api/auth` возвращает управляемое русское `Неверный пароль.` без HTTP-ошибки для обычного отказа                                                                                                     | QA `.69`: пустая форма держит `Продолжить` disabled; неверный пароль показывает `Неверный пароль.`; реальный пароль открывает `/chat/new`; `/api/auth-check` подтверждает вход и активную лицензию; browser console/network и свежие workspace/gateway логи чистые | FIXED  |
| Chat / голосовой ввод     | В Chromium кнопка `Голосовой ввод` была видимой, но короткий клик мог не давать пользователю ни состояния, ни понятной ошибки                                                                                                                    | Workspace поднят до `2.3.0-comandos.70`; в hook диктовки добавлена защита старта SpeechRecognition и русская ошибка, если браузер не запускает голосовой ввод                                                                                   | QA `.70`: клик по `Голосовой ввод` показывает `Не удалось распознать голос. Попробуйте ещё раз.`; browser console/page errors/failed requests/HTTP 4xx/5xx и свежие workspace/gateway логи чистые                                  | FIXED  |
| Chat / правая панель      | Закрытая правая панель агента оставляла скрытую кнопку `Переключить панель инспектора` в дереве доступности. Автотест и клавиатурная навигация видели кнопку, но мышью её нажать было невозможно, потому что панель имела ширину 0 и была вне экрана | Workspace поднят до `2.3.0-comandos.71`; закрытый desktop-`aside` правой панели теперь получает `aria-hidden` и `inert`, а инспектор доступен только после явного открытия панели агента                                                         | QA `.71`: до открытия панели кнопка инспектора недоступна; после `Открыть панель агента` инспектор нажимается, вкладки инспектора переключаются, `Скрыть панель агента` снова убирает скрытые кнопки из дерева доступности; browser/network/logs чистые | FIXED  |
| Chat / файловая панель    | Закрытая файловая панель оставляла невидимые `Обновить`/`Загрузить`/`Новый файл` разложенными поверх рабочей области. Автотест видел title-кнопки, но реальный клик попадал в шапку чата                                                         | Workspace поднят до `2.3.0-comandos.73`; файловый `aside` получил `aria-hidden`, `inert` и `overflow-hidden`, поэтому закрытая панель больше не оставляет скрытые кликабельные controls поверх чата                                             | QA `.73`: закрытая панель не даёт верхних файловых кнопок; после `Показать файлы` полный цикл `Обновить` -> `Новый файл` -> `Загрузить` -> `Скачать` -> `Удалить` прошёл без browser/network/log ошибок                              | FIXED  |
| Настройки / боковое меню   | На desktop-страницах настроек декоративная подложка бокового меню была настоящей кнопкой `Свернуть боковое меню` размером примерно `300x1000`. Пользователь её не видит как отдельное действие, но автопроверки и клавиатурная навигация считали её мёртвой кнопкой | Workspace поднят до `2.3.0-comandos.114`; подложка стала декоративным `div aria-hidden="true"` с `pointer-events-none`, а не кликабельной кнопкой                                                                                                  | QA и main `.114`: вход через меню пользователя и нижнюю шестерёнку открывает `/settings`; 9 разделов настроек открываются, `Показать запасную модель` раскрывается, подозрительных `Свернуть боковое меню` controls нет, browser console/page/network и свежие workspace/gateway логи чистые | FIXED  |
| Настройки / числовые поля  | Широкий скан main `.114` нашёл видимые `input type=number` без прямого доступного имени на `/settings` и `/settings/providers`. Визуально поля были понятны рядом с текстом, но автопроверка и клавиатурная навигация видели слабое имя поля                     | Workspace поднят до `2.3.0-comandos.115`; числовые поля терминала, агента, умного выбора модели и performance-настроек провайдера получили явные русские `aria-label`                                                                               | QA и main `.115`: широкий Playwright-проход 17 маршрутов подтвердил `unnamedNumberInputs=[]`, старые OAuth-тексты не появляются, `/api/update/status` показывает `current`; свежие workspace/gateway логи без ошибок                   | FIXED  |
| Настройки / голос          | Широкий live-скан `.122` нашёл в разделе `Голос` пользовательские английские/служебные подписи распознавания речи: `Local (Whisper)`, размеры `tiny/base/small/medium/large`, старые имена `STT` и placeholder `auto` | Workspace поднят до `2.3.0-comandos.123`; видимые подписи и `aria-label` распознавания речи переведены на русский, а сохранённые значения конфига `local`, `tiny/base/...` оставлены прежними | QA и main `.123`: `/settings?section=voice` проверен через Playwright; provider/model options показывают русские подписи, старые строки отсутствуют, browser console/network и свежие workspace/gateway логи чистые | FIXED  |
| Настройки / голос          | Широкий live-скан `.128` показал, что размеры локальной модели распознавания всё ещё видны пользователю как `Очень маленькая (tiny)`, `Базовая (base)`, `Малая (small)`, `Средняя (medium)`, `Большая (large)` | Workspace поднят до `2.3.0-comandos.129`; видимые подписи размеров модели стали полностью русскими, реальные значения конфига `tiny/base/small/medium/large` сохранены без изменения | QA и main `.129`: `/settings?section=voice` проверен через Playwright через Caddy; опции select: `Очень маленькая`, `Базовая`, `Малая`, `Средняя`, `Большая`; browser console/page/HTTP 5xx и свежие логи чистые | FIXED  |
| Agora                     | Широкий live-скан `.123` нашёл, что `/agora` выглядит как английская демо-заготовка: `the first AI agent community`, `{n} online`, `Your profile`, `Say something to the room...`, а после клика — английские профиль, чат, подсказки и mock-реплики | Workspace поднят до `2.3.0-comandos.124`; шапка, title страницы, список участников, профиль, статусы, аватары, чат, подсказка движения, mock-профили и реплики переведены на русский | QA и main `.124`: Playwright нажал `Открыть свой профиль`, статус, профиль участника, `Поприветствовать` и отправку сообщения; старые английские строки отсутствуют, browser console/page/HTTP 5xx и свежие workspace/gateway логи чистые | FIXED  |
| Файлы / Monaco            | Широкий live-скан `.124` нашёл на `/files` видимое служебное поле Monaco `textarea.ime-text-area` без доступного имени. Для пользователя это не отдельная кнопка, но автопроверка и клавиатурная навигация видели пустое рабочее поле | Workspace поднят до `2.3.0-comandos.125`; добавлен общий helper `labelMonacoTextareas`, файловый редактор получает имя `Редактор файлов`, редактор памяти — `Редактор памяти` | QA и main `.125`: `/files` проверен через Playwright; видимый Monaco textarea имеет `aria-label/title=Редактор файлов`, пустых видимых textarea нет, browser console/HTTP 5xx и свежие workspace/gateway логи чистые | FIXED  |
| Профили / ошибки API      | Ручной сценарий `/profiles` на `.125` показал, что если именованный профиль стал активным, прямой delete мог вернуть технический HTTP 500 с английской причиной `Cannot delete the active profile` вместо управляемого отказа | Workspace поднят до `2.3.0-comandos.126`; ожидаемые ошибки профилей нормализуются в `ok:false` с русским текстом без HTTP 500: активный профиль, отсутствующий профиль, дубль имени, неверное имя и запрет на `default` | QA и main `.126`: API-сценарий создал временный профиль, активировал, получил `Активный профиль нельзя удалить.`, восстановил `default`, удалил временный профиль; UI-кнопки QA `Создать`/`Детали`/`Переименовать`/`Активировать`/`Удалить` прошли без browser/server ошибок | FIXED  |
| MCP / route gate           | Ручной сценарий `/mcp` на `.126` показал, что при `mcp=false` и `mcpFallback=false` маршрут застревал на общей заглушке серверной части. Пользователь не мог открыть каталог и источники, хотя `/api/mcp` отдавал управляемый `capability_unavailable` | Workspace поднят до `2.3.0-comandos.127`; общий feature-gate убран, полный MCP-экран открывается всегда, а опасные действия `Добавить сервер` и `Установить` остаются disabled с русской причиной | QA и main `.127`: `/mcp` открывает полный экран, вкладка `Каталог` и `Источники` доступны, `Добавить сервер`/`Установить` disabled; `/api/gateway-status` и `/api/mcp` возвращают 200; browser console/HTTP 5xx и свежие логи чистые | FIXED  |
| MCP / источники каталога   | После `.127` Playwright нашёл, что форма `Добавить источник` видима, но поля не имеют прямых русских доступных имён, а строки источников могут показывать `built-in` и `generic-json` | Workspace поднят до `2.3.0-comandos.128`; поиск, фильтр и поля формы источника получили русские `aria-label`, а строки источников показывают русские значения `встроенный`, `Обычный JSON`, `Официальный`, `Сообщество`, `Не проверен` | QA и main `.128`: Playwright нажал `/mcp` -> `Каталог` -> `Источники` -> `Добавить источник`; все 6 полей находятся по русскому имени, ошибки пустой формы русские, старые `built-in`/`generic-json` не видны, browser console/page/HTTP 5xx и свежие логи чистые | FIXED  |
| Терминал / xterm input     | После `.115` скан доступных имён нашёл видимое служебное поле xterm с английским именем `Terminal input`. Мышью это не отдельная кнопка, но для автопроверки и клавиатурной навигации это выглядело как непереведённый рабочий ввод панели                       | Workspace поднят до `2.3.0-comandos.116`; общий helper `hideXtermMeasureElements` теперь назначает `.xterm-helper-textarea` русские `aria-label` и `title`: `Ввод терминала`                                                                       | QA и main `.116`: Playwright вошёл через `/api/auth`, открыл `/terminal`, подтвердил `ariaLabel/title=Ввод терминала`, `Terminal input` отсутствует в DOM/видимом тексте; browser page/HTTP 5xx и свежие workspace/gateway логи чистые | FIXED  |
| Вторичный слой controls     | В редких состояниях и дополнительных панелях оставались английские имена действий: `Attach image`, `Mobile navigation`, `Undo last message`, `Memory Files`, `Analytics period`, `Previous`, `Next`, `Approvals`, `Agent options`, `View Output`, `Pause`, `Kill session` | Workspace поднят до `2.3.0-comandos.117`; русифицированы подписи вложений, мобильной навигации, undo/clear чата, usage meter, файлов памяти, периода аналитики, календаря, approvals и панелей агентов                               | QA и main `.117`: широкий Playwright-проход 17 маршрутов подтвердил отсутствие старых строк в видимых controls; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без warning/error/failed/exception   | FIXED  |
| Скрытый слой рабочих панелей | После `.117` статический поиск нашёл следующий слой английских строк в overview/run console/dashboard/onboarding: `Configure`, `View Output`, `Send directive...`, `Stream`, `Timeline`, `Usage peaked`, `Cache reads` | Workspace поднят до `2.3.0-comandos.118`; русифицированы обзор миссии, карточки агентов, вкладки/подтверждения/отчёты run console, dashboard insights и подсказка монитора расхода | QA и main `.118`: широкий Playwright-проход 18 маршрутов подтвердил отсутствие старых строк в видимом тексте; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без warning/error/failed/exception | FIXED  |
| Настройки / провайдеры      | Живой проход `.118` показал, что маршрут `/settings/providers` работает, но title страницы всё ещё английский: `Provider Setup`, а onboarding смешивает русский текст со словом `providers` | Workspace поднят до `2.3.0-comandos.119`; title маршрута стал `Провайдеры моделей`, onboarding-подсказка использует русское `провайдеры` | QA и main `.119`: `/settings/providers` показывает `Провайдеры моделей — COMANDOS AI Workspace`; старые строки не найдены, browser console/network и свежие workspace/gateway логи чистые | FIXED  |
| HermesWorld / бронирование  | Живой проход `.120` нашёл HTTP 500 на `GET /api/hermesworld/reservations`, потому что на QA/main не настроены Supabase/Resend, а страница при этом выглядела как рабочая форма бронирования | Workspace поднят до `2.3.0-comandos.121`; API счётчика возвращает управляемый `200` со статусом `enabled=false`, форма отключена до настройки сервиса и показывает русское состояние подготовки | QA и main `.121`: `/reserve` и `/api/hermesworld/reservations` проверены через Playwright/API; кнопка `Бронирование пока не подключено` disabled, HTTP 4xx/5xx и browser console errors отсутствуют, свежие workspace/gateway логи чистые | FIXED  |
| Сайдбар / группы меню       | Широкий live-скан `.121` нашёл `Основное` и `Знания` как маленькие кликабельные ссылки на последний посещённый раздел. Визуально это заголовки групп, поэтому такое действие было скрытым и непонятным | Workspace поднят до `2.3.0-comandos.122`; заголовки групп стали обычным текстом, а сворачивание осталось отдельной кнопкой `Свернуть/Развернуть Основное` и `Свернуть/Развернуть Знания` | QA и main `.122`: `/chat/new`, `/chat/main`, `/dashboard` проверены через Playwright; `Основное`/`Знания` больше не находятся как `button/a/role=button/link`, кнопки сворачивания нажимаются, browser console/network и свежие workspace/gateway логи чистые | FIXED  |
| Сайдбар / рой               | Широкий live-скан `.129` нашёл повторяющиеся слишком маленькие зоны клика: кнопки сворачивания групп сайдбара были `16x16`, а кнопки настроек агентов в рое сжимались до `16x32`/`17x32` | Workspace поднят до `2.3.0-comandos.130`; кнопки сворачивания групп получили стабильную зону `32x32`, а кнопки настроек агентов перестали сжиматься во flex-карточке | QA и main `.130`: Playwright проверил `/dashboard`, `/chat/new`, `/swarm`, `/swarm2`; кнопки имеют минимум `32x32`, сворачивание и открытие настроек агента работают, browser console/page/HTTP 5xx и свежие user-журналы чистые | FIXED  |
| Рой / фильтры               | Широкий live-скан `.131` нашёл маленькие настоящие кнопки режимов маршрутизации `авто`/`один агент`/`всем` высотой `23px` и фильтры статуса агентов `ВСЕ`/`В РАБОТЕ`/`ПРОВЕРКА`/`СТОП`/`ГОТОВО` высотой `18px` на `/swarm` и `/swarm2` | Workspace поднят до `2.3.0-comandos.132`; режимы маршрутизации и фильтры статуса получили стабильную высоту `32px`, сохранили прежнюю логику выбора и не стали крупнее по смыслу, чем окружающая панель | QA и main `.132`: Playwright кликнул все режимы и фильтры на `/swarm` и `/swarm2`; размеры стали `49-94x32`, browser console/page/HTTP 5xx и свежие user-журналы чистые | FIXED  |
| Сайдбар / настройки         | Широкий live-скан `.132` нашёл, что нижняя общая кнопка `Настройки` на большинстве разделов сжималась до `28x28`. Действие работало, но зона клика была меньше принятого размера для панели | Workspace поднят до `2.3.0-comandos.133`; нижняя кнопка `Настройки` получила стабильный размер `32x32` без изменения маршрута и логики открытия настроек | QA и main `.133`: Playwright кликнул нижнюю `Настройки` на `/chat/new`, `/dashboard`, `/files`, `/tasks`, `/settings`, `/playground`; везде `32x32`, клик открывает `/settings`, browser console/page/HTTP 5xx и свежие user-журналы чистые | FIXED  |
| Терминал                  | Кнопка AI-анализа ходила в несуществующий `/api/debug-analyze`                                                                                                                                                                                   | Добавлен локальный безопасный обработчик анализа терминального вывода                                                                                                                                                                           | Кнопка `AI-анализ терминала` показывает русскую диагностику без 404/500                                                                                                                                                             | FIXED  |
| Терминал                  | Кнопка новой вкладки требовала проверки                                                                                                                                                                                                          | Проверено создание второй вкладки                                                                                                                                                                                                               | После клика виден `Терминал 2`, ошибок нет                                                                                                                                                                                          | OK     |
| Терминал                  | При переходах и закрытых PTY-сессиях `/api/terminal-resize` и `/api/terminal-input` могли давать 404/502 в browser console                                                                                                                       | Устаревшие terminal session теперь отвечают мягко: resize — `ok=true, attached=false`, input — `ok=false, attached=false`, без HTTP-ошибки                                                                                                      | `clawd` обновлён до `.17`; прямые POST к stale session вернули 200, свежие логи без новых ошибок                                                                                                                                    | FIXED  |
| Терминал                  | При длинном проходе по меню после `/terminal` на `.18` снова появились 502 на `/api/terminal-resize`/`/api/terminal-stream`, а в логах — `MaxListenersExceededWarning`                                                                           | TerminalWorkspace теперь не инициализирует PTY и не resize-ит терминал, пока раздел скрыт; при уходе со страницы отменяет stream-reader, а при возврате переподключается к существующей PTY                                                     | `clawd` обновлён до `.19`; live-обход 14 пунктов меню после `/terminal` прошёл без console errors, HTTP 4xx/5xx и новых `MaxListenersExceededWarning`                                                                               | FIXED  |
| Файлы                     | Нужно было проверить реальные действия файлов                                                                                                                                                                                                    | Проверены новый файл, редактирование, сохранение, upload, context menu, download item, delete                                                                                                                                                   | Временный файл создан, сохранён и удалён, ошибок нет                                                                                                                                                                                | OK     |
| Файлы                     | Правый клик по файлу мог не показывать `Переименовать`/`Скачать`/`Удалить`: глобальный `contextmenu`-закрыватель гасил меню сразу после открытия                                                                                                 | Workspace поднят до `2.3.0-comandos.48`; правый `mousedown/contextmenu` на файле останавливает всплытие и не отдаёт событие глобальному закрытию                                                                                                | QA `.48`: через UI правый клик по файлу показал меню, `Удалить` вызвал confirm, файл исчез из UI и `/api/files`; browser console/network и свежие workspace/gateway логи чистые                                                     | FIXED  |
| Dashboard / Settings      | Оставались английские пользовательские подписи                                                                                                                                                                                                   | Русифицированы видимые подписи, tooltip и статусы провайдеров                                                                                                                                                                                   | Маршрутный обход не нашёл `Key required`, `Key set`, `Start OAuth`, `Usage trend`, `No analytics usage`                                                                                                                             | FIXED  |
| Gateway-зависимые разделы | Сообщения о недоступной серверной части были частично английскими                                                                                                                                                                                | Русифицированы сообщения возможностей gateway                                                                                                                                                                                                   | `/jobs`, `/mcp` показывают понятное объяснение, что нужен Hermes Agent gateway                                                                                                                                                      | FIXED  |
| Подключение               | Кнопка `Сохранить и проверить` сохраняла адреса, но не показывала пользователю результат проверки gateway, хотя текст обещал проверку                                                                                                            | Workspace поднят до `2.3.0-comandos.67`; после сохранения и сброса UI запрашивает `/api/connection-status`, показывает русское `Подключение: ...`, а `/api/connection-settings` отдаёт русские ошибки URL                                    | QA `.67`: `Сохранить и проверить` показал `Сохранено. Подключение: Подключено...`; `Сбросить` вернул источник к env и показал проверку; invalid URL вернул русскую 400; browser/network/logs чистые                               | FIXED  |
| Язык                      | Селектор языка показывал 11 вариантов, но код `getLocale`/`setLocale` принудительно оставлял `ru`, поэтому выбор любого другого языка был мёртвым действием                                                                                       | Workspace поднят до `2.3.0-comandos.68`; вкладка честно показывает отключённый русский режим и объясняет, что остальные переводы пока выключены                                                                                                | QA `.67` воспроизвёл FAIL: select активен, 11 языков; QA `.68` подтвердил: select disabled, единственный option `Русский`, browser console/network и свежие workspace/gateway логи пустые                                      | FIXED  |
| Обновления                | После пуша версия manifest могла не измениться или raw GitHub мог отдать старый кэш                                                                                                                                                              | Workspace поднят до `2.3.0-comandos.11`; update checker сначала превращает `main` в конкретный SHA через `git ls-remote`                                                                                                                        | Raw manifest по SHA отдаёт `.11`; `update-system` тесты проходят                                                                                                                                                                    | FIXED  |
| Обновления                | Крестик в карточке обновления скрывал конкретную версию навсегда в `localStorage`                                                                                                                                                                | Workspace поднят до `2.3.0-comandos.12`; скрытие теперь действует 24 часа, старые вечные маркеры игнорируются                                                                                                                                   | `update-center-notifier` тесты проходят; `clawd` API показал `2.3.0-comandos.7 → 2.3.0-comandos.11` до фикса                                                                                                                        | FIXED  |
| Обновления                | Кнопка обновления успевала перезапустить панель до ответа API; state записывал старую версию из lock                                                                                                                                             | `comandos-hermes.lock` поднят до `2.3.0-comandos.12`; restart delay вынесен в `COMANDOS_WORKSPACE_RESTART_DELAY_SECONDS` с дефолтом 30 сек; отложенный restart отсоединён через `nohup`                                                         | На `clawd` повторный API-вызов вернул JSON `ok=true`; после отложенного рестарта служба активна, installed state `.12`, update status `current`                                                                                     | FIXED  |
| Обновления                | После правки установщика без bump версии установленная панель не увидела бы обновление                                                                                                                                                           | Workspace поднят до `2.3.0-comandos.13`, manifest/lock/package синхронизированы                                                                                                                                                                 | `clawd`: status показал `.12 → .13`, POST `/api/update/workspace` вернул `ok=true`, после рестарта services active, update status `current`                                                                                         | FIXED  |
| Обновления                | При сетевом обрыве, рестарте панели или нечитаемом ответе update UI мог показать сырой `Failed to fetch`, `Unexpected token` или английскую backend-ошибку                                                                                       | Workspace поднят до `2.3.0-comandos.49`; ошибки update-кнопки нормализуются в понятный русский текст, нечитаемый JSON считается управляемым рестартом панели                                                                                    | QA `.48 → .49`: карточка видна, `Скрыть на 24 часа` работает, кнопка `Обновить` довела сервис до `.49`; после рестарта статус `current`, карточка исчезла, сырого error-текста нет, user-сервисы active, свежие логи без ошибок     | FIXED  |
| Обновления                | Полноразмерные backup-и update-скрипта забивали диск QA и могли оставить битую server-сборку                                                                                                                                                     | Workspace поднят до `2.3.0-comandos.57`; backup исключает тяжёлые каталоги, а update не принимает сборку с пустыми или невалидными server JS                                                                                                    | QA `.57`: `zero_server_js=0`, `node --check` чистый, user-сервисы active, свежие логи без ошибок                                                                                                                                    | FIXED  |
| Обновления                | Toast-уведомления и карточка обновлений могли перекрывать рабочие кнопки панели, включая кнопки задания                                                                                                                                          | Workspace поднят до `2.3.0-comandos.60`; тело уведомлений пропускает клики, кликабельными остаются только реальные действия `Обновить`, `Скрыть` и крестик toast                                                                                | QA `.60`: полный Playwright-прогон `/jobs` нажал `Пауза`/`Возобновить` под уведомлениями без intercepted-click ошибок; browser console/network чистые                                                                               | FIXED  |
| Обновления                | После ручного обновления QA `/api/update/status` мог показывать Workspace как `installKind=unknown`, `state=unsupported`, потому что managed-установка без `.git` откатывалась к старой git-проверке, если GitHub raw не успевал отдать manifest | Workspace поднят до `2.3.0-comandos.61`; manifest кэшируется в `.runtime/update-manifest-cache.json`, update script сам сохраняет кэш из свежего клона, а managed-статус явно отдаёт `installKind=managed`                                      | QA `.61`: `/api/update/status` вернул `version=latestVersion=2.3.0-comandos.61`, `installKind=managed`, `state=current`, `reason=null`; package, installed-state и manifest-cache синхронны; сервисы active, свежие логи без ошибок | FIXED  |
| Провайдеры                | `/settings/providers` мог показывать `0 пров.` и бесконечное `Загружаю провайдеры из Hermes Agent...`, а часть настроек ходила в несуществующие `/api/config-get` и `/api/config-patch`                                                          | Workspace поднят до `2.3.0-comandos.63`; карточки подключённых провайдеров строятся из `/api/hermes-config` без ожидания локального discovery; legacy-маршруты настроек возвращают JSON и сохраняют `path/value`/`raw`-патчи                    | Unit/lint OK; QA `.63`: DeepSeek виден, Codex wizard ведёт в `CLI-вход`, `/api/config-get` и `/api/config-patch` возвращают JSON; найден отдельный стартовый тормоз `/api/update/status`                                | FIXED  |
| Обновления                | Первый заход в панель мог зависать примерно на 30 секунд: `/api/update/status` синхронно ходил в GitHub raw/commit API и всегда проверял Hermes Agent через git fetch, блокируя остальные API-запросы Node-процесса                                        | Workspace поднят до `2.3.0-comandos.66`; статус обновлений читает локальный manifest-кэш без сетевого ожидания, обновляет удалённый manifest в фоне, в managed-режиме берёт агент из state без git fetch и не показывает ложное обновление при совпавшем ref | QA `.66`: `/api/update/status` отвечает примерно за 1 сек., первый заход в `settings/providers` проходит, агент не показывает ложное обновление, свежая повторная проверка browser/API/logs без ошибок                  | FIXED  |
| Задания                   | Кнопки `Пауза`, `Возобновить`, `Запустить сейчас`, `Удалить` и сохранение формы не всегда сразу обновляли карточку в списке                                                                                                                      | Workspace поднят до `2.3.0-comandos.54`; successful actions обновляют React Query cache и затем делают refetch                                                                                                                                  | QA `.60`: после кликов состояние карточки меняется сразу; API подтверждает pause/resume/delete                                                                                                                                      | FIXED  |
| Задания                   | Редактирование задания не сохраняло поле `Навыки`                                                                                                                                                                                                | Workspace поднят до `2.3.0-comandos.55`; `cron edit` получает `--skill`, а очистка поля удаляет навыки                                                                                                                                          | QA `.60`: создано задание с 2 навыками, затем отредактировано до 3 навыков; API вернул `skills=["research","writing","synthesis"]`                                                                                                  | FIXED  |
| Задания                   | `Запустить сейчас` только ставил задачу на ближайший tick, но сам tick мог не выполнить работу из-за hooks approval                                                                                                                              | Workspace поднят до `2.3.0-comandos.58`; `cron run` и фоновый `cron tick` запускаются с `--accept-hooks`                                                                                                                                        | QA `.60`: после клика `Запустить сейчас` поле `last_run_at` обновилось до `2026-06-05T18:36:52.199206+00:00`, свежие логи без ошибок                                                                                                | FIXED  |
| Установка                 | В уроке пользователь мог заполнить `comandos-hermes.env.example`, а агент дальше проверял пустой `comandos-hermes.env`                                                                                                                           | Workspace поднят до `2.3.0-comandos.13`; `deploy.sh` берёт заполненный example, если основной env не проходит проверку; тексты урока обновлены                                                                                                  | Чистая установка из GitHub отдаёт новые тексты; изолированный fake-SSH прогон: основной env падает, example проходит, deploy выбирает example                                                                                       | FIXED  |
| Задачи                    | На чистом VPS может отсутствовать системный `sqlite3`, из-за чего backend задач пишет `spawnSync sqlite3 ENOENT`                                                                                                                                 | Workspace поднят до `2.3.0-comandos.15`; `deploy.sh` ставит `sqlite3`, update script доставляет его на уже установленной панели, а backend временно уходит на локальную доску, если зависимость ещё отсутствует                                 | `bash -n` для install/update scripts OK; `kanban-backend` тест добавлен для fallback без `sqlite3`; `clawd` обновлён до `.15`, tasks API не падает                                                                                  | FIXED  |
| Панель агента             | Справа показывался пустой и ложный `OpenAI`, хотя активная модель — `deepseek-chat`                                                                                                                                                              | Workspace поднят до `2.3.0-comandos.16`; панель агента теперь берёт модель из `/api/claude-config`, а провайдер расхода выбирает только при наличии реальных progress-строк                                                                     | Тест `agent-usage-helpers` проверяет, что badge-only OpenAI не выбирается; `pnpm build` OK                                                                                                                                          | FIXED  |
| Обновления                | Модалка показывала урезанную версию `2.3.0-c → main` и могла всплывать от старых pending notes при обычном открытии                                                                                                                              | Workspace поднят до `2.3.0-comandos.16`; версии больше не режутся как SHA, managed release notes пишут версию вместо `main`, старые notes нормализуются и не открываются сами на routine status poll                                            | Тесты `update-center-notifier` и `update-system` проверяют формат версий и нормализацию `main → 2.3.0-comandos.*`; `pnpm build` OK                                                                                                  | FIXED  |
| Навыки                    | Кнопки `Установить`, `Удалить` и toggle выглядели активными, хотя текущий managed/zero-fork API не поддерживал эти действия; прямой вызов установки мог вернуть техническую ошибку JSON-парсинга                                                 | Workspace поднят до `2.3.0-comandos.20`; `/api/skills` отдаёт `actions`, UI отключает недоступные действия с русским объяснением, direct install/uninstall/toggle возвращают управляемые русские 501                                            | `clawd` обновлён до `.20`; `/api/skills` вернул `install/uninstall/toggle=false`; прямые install/toggle вернули русские 501; Playwright `/skills` не нашёл активных `Установить/Удалить`, console/network чистые                    | FIXED  |
| MCP                       | Внутренний экран MCP мог показывать активные `Добавить сервер` и `Установить`, если route открыт при `mcp=false` и `mcpFallback=false`                                                                                                           | Workspace поднят до `2.3.0-comandos.44`; MCP-действия в режиме `off` отключаются, footer показывает `режим: недоступно`, а экран объясняет причину по-русски                                                                                    | QA `.44`: общий feature-gate не пускает к мёртвым MCP-кнопкам; `/api/mcp` возвращает HTTP 200 `capability_unavailable`; browser console/network и свежие логи пустые                                                                | FIXED  |
| Память / База знаний      | Кнопка `GitHub-репозиторий` в настройках базы знаний нажималась, но режим мог тут же откатываться обратно на `Локальная папка`: поздний ответ `/api/knowledge/config` перетирал ручной выбор пользователя                                        | Workspace поднят до `2.3.0-comandos.96`; ручной выбор источника теперь помечается как пользовательское изменение, а поздний ответ API не перезаписывает draft; оставшиеся английские подписи экрана базы знаний русифицированы                  | QA `.96`: `База знаний` -> `Настройки` -> `GitHub-репозиторий` показывает `Репозиторий`/`Ветка`/`Подпапка`; возврат на `Локальная папка` показывает `Путь к папке`; `Граф связей` открывается; browser/network/logs чистые          | FIXED  |
| Свёрнутое меню / терминал | В свёрнутой панели и верхней зоне оставались видимые controls без понятного имени: иконки меню были подписаны только визуально, а вкладка терминала на верхней кромке читалась как пустая кнопка                                                  | Workspace поднят до `2.3.0-comandos.97/.98`; пункты свёрнутого меню и кнопка пользователя получили `aria-label/title`, вкладка терминала и её закрытие подписаны как `Вкладка терминала: ...` и `Закрыть вкладку терминала: ...`                | QA `.98`: Playwright после входа сворачивает меню, собирает все видимые `button/a/role=button` в левой и верхней зоне; пустых имён нет, `Открыть меню` раскрывает меню обратно, browser/network/logs чистые                       | FIXED  |
| Настройки / Switch        | Общий скан `.98` нашёл на `/settings` две видимые 1x1 checkbox без имени. Это скрытые input от Base UI Switch: видимый переключатель был подписан, но внутренний input оставался кликабельным для сканера                                         | Workspace поднят до `2.3.0-comandos.99`; общий компонент `Switch` оборачивает Base UI root и отключает pointer-events у `input[aria-hidden="true"]`, не меняя видимый переключатель и его обработчики                                           | QA `.99`: `/settings?section=chat` нажал switch `Показывать сообщения инструментов` туда-обратно; широкий скан 17 маршрутов прошёл 795 controls без empty/tiny/badText, browser/network/logs чистые                              | FIXED  |
| HermesWorld / Playground  | `/playground` оставался смешанным: часть игровых кнопок, диалогов NPC, инвентаря, карты, хранилища основателя, настроек, миникарты, online-индикатора, админ-панели и мобильного ввода терминала была на английском или с техническими подписями; после `.100` выяснилось, что кнопка `Запустить в панели` всё ещё открывает внешний iframe, после `.101/.102` — что стартовая карточка и HUD локальной игры всё ещё показывают отдельные английские CTA/метки, после `.103` — что 3D-мир падает на NPC из-за `isNear` до инициализации, после `.104` — что HUD/чат/журнал ещё показывали отдельные английские статусы, а HEAD-проба отсутствующих GLB давала 404; живая QA `.106/.107/.108` нашла ещё два перекрытия: открытая панель инвентаря перекрывала правый rail и нижний utility dock; у utility-кнопок снимка/fullscreen/copy не было нормального имени для автопроверки | Workspace поднят до `2.3.0-comandos.108`; русифицированы видимые подписи, `aria-label`, `title`, toast-сообщения, статусы, действия, квесты, награды, настройки, fallback-тексты, стартовый экран, HUD цели, журнал, карта, чат, вывески, помещения, подписи NPC, зоны и базовые реплики; `Запустить в панели` монтирует локальный `PlaygroundScreen`, внешний сайт оставлен только ссылкой `Открыть отдельно`; public-запуск скрывает админ-панель; runtime-крэш NPC исправлен; GLB fallback больше не шумит 404; right rail и utility dock сдвигаются влево при открытой боковой панели; utility-кнопки получили русские `aria-label` | Локально: version-lock test, `git diff --check`, `pnpm build`, `node --check dist/server/server.js` OK. QA `.108`: installed-state/package `.108`, `/playground` HTTP 200; Playwright вошёл через прямой API auth, открыл `/playground`, нажал `Запустить в панели`, закрыл предупреждение `Продолжить без изменений`; живыми кликами проверены `Инвентарь`, `Журнал заданий`, `Карта`, `Настройки`, `Режим фокуса`, `Сделать снимок мира`, `Открыть полноэкранный режим`, `Копировать ссылку`, `Повторить озвучку мира`, `Выключить озвучку`, `Выключить звук`, `Настроить аватар`; browser console/page/network и свежие service logs чистые | FIXED  |
| Навыки                    | В карточках навыков оставались английские описания внешнего каталога и категории `models`/`skills`/`evaluation`, из-за чего общий аудит видел пользовательский английский в рабочем разделе                                                   | Workspace поднят до `2.3.0-comandos.109/.112`; известные описания и категории русифицированы, неизвестные внешние описания заменены честным русским предупреждением о ручной проверке перед установкой                                         | QA `.112`: `/skills` открывается без console/network ошибок; точный скан реальных кнопок не нашёл английских `Install/Uninstall/models/skills/evaluation`; широкий скан шумит только на `tabpanel` из-за имён навыков/авторов       | FIXED  |
| Настройки / Switch        | После старого исправления `.99` внутренний Base UI checkbox всё ещё мог попадать в аудит как видимый `input value=on`                                                                                                                          | Workspace поднят до `2.3.0-comandos.110`; внутренний `input[aria-hidden="true"]` общего Switch скрыт от разметки, а видимый переключатель остался рабочим                                                                                       | QA `.110/.112`: `/settings` не показывает 1x1 `input on`; переключатели возвращают исходные значения; точный скан кнопок чистый                                                                                                   | FIXED  |
| Чат / Рой                 | Общий проход после HermesWorld нашёл пользовательские английские подписи `Local Chat` и `KM Agent`, а у полосы контекста была слишком маленькая кликабельная высота                                                                            | Workspace поднят до `2.3.0-comandos.111/.112`; пустая локальная сессия отображается как `Локальный чат`, `KM Agent` нормализован в `Агент знаний`, контекстная полоса увеличена до нормальной высоты                                           | QA `.112`: `/chat/main`, `/swarm`, `/swarm2` больше не дают `Local Chat`/`KM Agent` в точном скане реальных кнопок; browser console/page/network и свежие service logs чистые                                                     | FIXED  |
| Провайдеры                | Прямой вызов OAuth API для `openai-codex` мог вернуть общий технический тупик вместо понятного CLI-сценария                                                                                                                                      | Workspace поднят до `2.3.0-comandos.23`; `/api/oauth/device-code` для Codex возвращает русское объяснение с `codex login`, ошибки JSON/provider тоже русифицированы; `settings-dialog.tsx` очищен от lint-ошибок в чтении конфига               | `pnpm exec vitest run src/components/settings-dialog/settings-dialog.test.ts src/routes/api/-oauth-device-code.test.ts` OK; `pnpm exec eslint ...settings-dialog... oauth.device-code...` OK                                        | FIXED  |
| Провайдеры                | Кнопка `Сохранить` на `/settings/providers` могла тихо заменить реальный default provider `deepseek` на `custom`, потому что форма модели знала только `custom/openrouter/anthropic/openai`                                                      | Workspace поднят до `2.3.0-comandos.50`; форма берёт список провайдеров из общего каталога и сохраняет неизвестные provider id вместо принудительной замены на `custom`                                                                         | QA `.50`: до клика активен `deepseek/deepseek-chat`; после `Сохранить` activeProvider остался `deepseek`; Codex wizard дошёл до `CLI-вход` и `Перейти к проверке`; browser console/network и свежие workspace/gateway логи чистые   | FIXED  |
| Новый чат                 | Первая отправка из `/chat/new` сохраняла `user + assistant`, но UI оставался пустым или показывал только пользовательское сообщение                                                                                                              | Workspace поднят до `2.3.0-comandos.28`; `/api/history` отдаёт local portable history даже без sessions API, local messages имеют поле `text`, а frontend в portable-режиме refetch-ит backend-историю по реальному session id                  | Live Playwright на `.28`: `/chat/new` -> отправка -> `/chat/main`, UI показал QA-маркер 2 раза за 10 секунд; `/api/history` вернул `user + assistant`; console/network чистые; свежие systemd-логи без ошибок; QA-сессия удалена    | FIXED  |
| Сессии                    | Меню сессии показывало английский текст `Pin session` / `Unpin session`                                                                                                                                                                          | Workspace поднят до `2.3.0-comandos.51`; действие закрепления сессии подписано как `Закрепить сессию` / `Открепить сессию`                                                                                                                      | QA `.51`: меню `Настройки сессии` открывается, русский текст виден, английский не найден; browser console/network и свежие workspace/gateway логи без ошибок                                                                        | FIXED  |
| Рой                       | Старый сохранённый `swarm.yaml` продолжал показывать английские миссии и роли агентов на `/swarm2`, хотя новый шаблон уже был русским                                                                                                            | Workspace поднят до `2.3.0-comandos.52`; стандартные legacy-строки известных агентов нормализуются на русском при чтении ростера, пользовательский кастомный текст не перетирается                                                              | QA `.52`: `/swarm` и `/swarm2` показывают русские миссии агентов, старые фразы `Ship scoped...`, `Verify user-visible...`, `Decompose missions...` не найдены; browser console/HTTP 4xx/5xx и свежие workspace/gateway логи чистые  | FIXED  |
| Установка                 | Чистая параллельная установка на том же VPS могла перезаписать рабочий Caddy-блок `COMANDOS HERMES`                                                                                                                                              | Workspace поднят до `2.3.0-comandos.34`; `deploy.sh` поддерживает `COMANDOS_CADDY_BLOCK_ID`, по умолчанию оставляет старый блок, а QA-контур может использовать отдельный marker                                                                | `bash -n` для установочных скриптов OK; Python-проверка Caddy marker сохраняет основной блок и заменяет только QA-блок; `update-system` тесты и `pnpm build` OK                                                                     | FIXED  |
| Установка                 | При чистой установке SSH на VPS временно закрыл соединение до баннера, и `deploy.sh` падал с `Connection closed by ... port 22`                                                                                                                  | Workspace поднят до `2.3.0-comandos.34`; SSH-шаг deploy получил ограниченный retry с настраиваемыми `COMANDOS_SSH_RETRIES` и `COMANDOS_SSH_RETRY_DELAY_SECONDS`                                                                                 | Чистая установка `.30` показала повторы на `SSH remote install`; основной сервер не был изменён                                                                                                                                     | FIXED  |
| Установка                 | После всех неудачных SSH-повторов `deploy.sh` мог напечатать локальное «готово», хотя удалённая установка не стартовала                                                                                                                          | Workspace поднят до `2.3.0-comandos.34`; retry теперь вызывает явный `fail` после последней неудачной попытки и валидирует retry-переменные                                                                                                     | Серверная проверка после ложного успеха: QA-каталога нет, QA-порты не слушают, Caddy не изменён; `.31` уже падает честно без ложного успеха                                                                                         | FIXED  |
| Установка                 | Deploy делал серию быстрых подключений `ssh + scp + scp + scp + scp + ssh`, что провоцировало rate-limit sshd на VPS                                                                                                                             | Workspace поднят до `2.3.0-comandos.34`; payload, env, lock и remote installer упаковываются в один deploy bundle и отправляются одним SSH-потоком                                                                                              | `.32` дошла до remote install одним SSH-потоком, без SCP-серии                                                                                                                                                                      | FIXED  |
| Установка                 | При входе на VPS под sudo-пользователем новый `REMOTE_APP_USER` не создавался, потому что скрипт требовал root                                                                                                                                   | Workspace поднят до `2.3.0-comandos.34`; создание app-пользователя теперь идёт через `$SUDO useradd`                                                                                                                                            | `.33` создала `hermesqa` и поставила Hermes Agent                                                                                                                                                                                   | FIXED  |
| Установка                 | macOS xattrs попадали в deploy-архив и на VPS печатались предупреждения `LIBARCHIVE.xattr.com.apple.provenance`                                                                                                                                  | Workspace поднят до `2.3.0-comandos.34`; tar-архивы создаются с `--no-xattrs`                                                                                                                                                                   | `.33` дошла до remote install без предупреждений `LIBARCHIVE.xattr...`                                                                                                                                                              | FIXED  |
| Установка                 | После sudo-установки Hermes Agent CLI существовал у `REMOTE_APP_USER`, но `deploy.sh` проверял `/home/.../.local/bin/hermes` от SSH-пользователя и падал по правам                                                                               | Workspace поднят до `2.3.0-comandos.34`; `find_hermes_cli` ищет `command -v hermes` и явные пути через `as_app_user`                                                                                                                            | Серверная проверка: `/home/hermesqa/.local/bin/hermes` существует и виден от `hermesqa`; фикс готовится к повторной чистой установке                                                                                                | FIXED  |
| Установка                 | На sudo-доступе `deploy.sh` писал `.env`, `config.yaml`, update/state и Telegram-файлы прямым редиректом в каталоги `REMOTE_APP_USER`; при закрытой домашней папке установка падала с `Permission denied`                                        | Workspace поднят до `2.3.0-comandos.35`; файлы генерируются во временной папке и устанавливаются через `sudo install -o REMOTE_APP_USER -m ...`                                                                                                 | Чистая установка `.34` воспроизвела падение на `/home/hermesqa/.hermes/.env`; `.36` прошла эту точку и завершилась успешно                                                                                                          | FIXED  |
| Установка                 | При сбое после сборки `deploy.sh` помечал workspace установленным до проверки сервисов, затем retry повторял всю сборку, создавал backup с `node_modules/dist` и забивал диск (`ENOSPC`)                                                         | Workspace поднят до `2.3.0-comandos.36`; retry повторяет только SSH exit 255, installed-state пишется после `checks`, backup исключает `node_modules`, `dist`, `logs`, `.runtime`, а user-systemd проверяется через реальный `systemctl --user` | `.35` прошла сборку и воспроизвела `user-systemd`/`ENOSPC`; `.36` чисто установилась на QA-контур, сервисы active, основной live не задет                                                                                           | FIXED  |

## Проверенные маршруты

| Раздел           | URL                   | Итог                                                                                                                                                                  | Статус   |
| ---------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Панель           | `/dashboard`          | Открывается, кнопки верхнего уровня видны, ошибок нет                                                                                                                 | OK       |
| Меню             | `/dashboard`          | QA `.97/.98`: `Свернуть меню`/`Открыть меню`, все иконки навигации и кнопка пользователя имеют русские имена; безымянных controls в левой/верхней зоне нет             | FIXED    |
| Новый чат        | `/chat/new`           | Открывается, поле ввода доступно, отправка отключена при пустом сообщении; первая реальная отправка показывает ответ модели                                           | FIXED    |
| Существующий чат | `/chat/main`          | Открывается, кнопка `Новая сессия` переводит в `/chat/new`                                                                                                            | OK       |
| Файлы            | `/files`              | Открывается, файловое дерево, preview и контекстное меню работают; QA `.48` проверила удаление через настоящий правый клик                                            | FIXED    |
| Терминал         | `/terminal`           | Открывается, вкладки и AI-анализ работают                                                                                                                             | FIXED    |
| Задания          | `/jobs`               | QA `.60`: при живом gateway полный цикл cron-задания работает; при недоступном gateway показывает русское disabled-состояние                                          | OK       |
| Задачи           | `/tasks`              | Создание/редактирование/удаление задач работает                                                                                                                       | FIXED    |
| Оркестратор      | `/conductor`          | Основные кнопки и модальные окна открываются без ошибок                                                                                                               | OK       |
| Операции         | `/operations`         | Вкладки и создание агента открываются без ошибок                                                                                                                      | OK       |
| Рой              | `/swarm`              | Вкладки, настройки, добавление агента и навигация панелей работают                                                                                                    | OK       |
| Память           | `/memory`             | Вкладки `Память` и `База знаний` работают, пустое состояние понятно                                                                                                   | OK       |
| Память           | `/memory`             | QA `.96`: настройки базы знаний переключают `Локальная папка`/`GitHub-репозиторий`, граф открывается, старых английских подписей на проверенных состояниях нет        | FIXED    |
| Навыки           | `/skills`             | Вкладки, фильтры и pagination-состояния работают                                                                                                                      | OK       |
| Навыки           | `/skills`             | Недоступные install/uninstall/toggle действия честно disabled с русским объяснением                                                                                   | FIXED    |
| MCP              | `/mcp`                | QA `.44`: при живом gateway без MCP runtime показывает русское disabled-состояние; кнопки добавления/установки не доступны; API отвечает 200 `capability_unavailable` | DISABLED |
| Профили          | `/profiles`           | Импорт, создание, детали, ограничения default-профиля проверены                                                                                                       | FIXED    |
| Настройки        | `/settings`           | Вкладки и основные действия открываются без пользовательского английского мусора                                                                                      | FIXED    |
| Провайдеры       | `/settings/providers` | Codex CLI-сценарий и добавление провайдера проверены                                                                                                                  | FIXED    |
| HermesWorld      | `/playground`         | QA `.108`: локальный запуск в панели работает, предупреждение закрывается, правый rail и нижний utility dock кликаются при открытой боковой панели; проверенные игровые кнопки проходят без browser/network/server ошибок | FIXED    |
| HermesWorld      | `/reserve`            | QA/main `.121`: при отсутствующих Supabase/Resend страница не бьёт `500`, API счётчика отвечает `200`, форма честно disabled с русским текстом                         | FIXED    |

## Проверенные кнопки и действия

| Где             | Действие                                                                                                                                                                                                                | Ожидание                                                                                                         | Итог     |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------- |
| Сайдбар         | Логотип, новая сессия, чат, панель, файлы, терминал, задачи, оркестратор, операции, рой, память, навыки, MCP, профили                                                                                                   | Переход на нужный маршрут                                                                                        | OK       |
| Сайдбар         | `Поиск`                                                                                                                                                                                                                 | Открыть поиск                                                                                                    | OK       |
| Сайдбар         | `Свернуть меню`                                                                                                                                                                                                         | Свернуть/развернуть меню                                                                                         | OK       |
| Сайдбар         | `Основное` и `Знания`                                                                                                                                                                                                   | Не являются самостоятельными ссылками; сворачиваются только отдельными кнопками группы                            | FIXED    |
| Сайдбар         | `Пользователь`                                                                                                                                                                                                          | Открыть меню пользователя                                                                                        | OK       |
| Сайдбар         | `Настройки`                                                                                                                                                                                                             | Открыть `/settings`                                                                                              | OK       |
| Chat            | `Показать файлы` / `Скрыть файлы`                                                                                                                                                                                       | Переключить файловую панель                                                                                      | OK       |
| Chat            | `Аудит рабочей папки`                                                                                                                                                                                                   | Подставить готовый промпт в composer                                                                             | OK       |
| Chat            | `Записать правило в память`                                                                                                                                                                                             | Подставить готовый промпт в composer                                                                             | OK       |
| Chat            | `Создать файл`                                                                                                                                                                                                          | Подставить готовый промпт в composer                                                                             | OK       |
| Chat            | `Отправить сообщение` из `/chat/new`                                                                                                                                                                                    | Создать первую сессию и показать ответ модели без ручного обновления страницы                                    | FIXED    |
| Chat            | `Настройки чата`                                                                                                                                                                                                        | Открыть настройки текущей сессии                                                                                 | OK       |
| Chat / Sessions | `Настройки сессии` -> `Закрепить сессию`                                                                                                                                                                                | Открыть меню сессии с русскими действиями закрепления, переименования и удаления                                 | FIXED    |
| Chat            | `Голосовой ввод`                                                                                                                                                                                                        | Не падать без микрофона/разрешения браузера                                                                      | OK       |
| Chat            | `Голосовой ввод` / длинное нажатие микрофона                                                                                                                                                                            | Ошибки микрофона/STT показываются по-русски без сырого ответа провайдера                                         | FIXED    |
| Agent panel     | Индикатор модели и расхода                                                                                                                                                                                              | Не показывать пустой `OpenAI`, если у него нет данных расхода                                                    | FIXED    |
| Files           | `Новый файл`, `Загрузить`, сохранить, preview, context menu, удалить                                                                                                                                                    | Работа с временным файлом; правый клик по файлу открывает меню, `Удалить` удаляет файл из UI и API               | FIXED    |
| Terminal        | `Новая вкладка терминала`                                                                                                                                                                                               | Создать вкладку                                                                                                  | OK       |
| Terminal        | `AI-анализ терминала`                                                                                                                                                                                                   | Показать русскую диагностику                                                                                     | FIXED    |
| Jobs            | `Обновить список`, `Создать задание`, `Отмена`, `Создать`, `Показать историю запусков`, `Скрыть историю запусков`, `Пауза`, `Возобновить`, `Редактировать`, `Сохранить`, `Запустить сейчас`, поиск, `Удалить` + confirm | Полный цикл временного cron-задания через UI, API и проверку логов                                               | OK       |
| Tasks           | `Новая задача`, edit, save, delete                                                                                                                                                                                      | Полный цикл карточки                                                                                             | FIXED    |
| Profiles        | `Импорт из ClawBot`                                                                                                                                                                                                     | Открыть чат с промптом мигратора                                                                                 | OK       |
| Profiles        | `Создать профиль`, rename, activate, delete                                                                                                                                                                             | Полный цикл временного профиля                                                                                   | OK       |
| Profiles        | `Переименовать` у `default`                                                                                                                                                                                             | Disabled с объяснением                                                                                           | DISABLED |
| Settings        | Вкладки модель/агент/голос/экран/тема/чат/сигналы/язык                                                                                                                                                                  | Переключаются без ошибок                                                                                         | OK       |
| Settings        | `Показать запасную модель`, `Сохранить модель`, добавление ключа, custom provider validation                                                                                                                            | Не падают, показывают понятное состояние                                                                         | OK       |
| Settings        | `Пользователь` -> `Настройки`, нижняя кнопка `Настройки`, вкладки `Подключение`, `Модель и провайдер`, `Поведение агента`, `Голос`, `Экран`, `Тема`, `Чат`, `Сигналы`, `Язык`                                         | Открываются без мёртвых overlay-кнопок, технического текста и browser/server ошибок                              | FIXED    |
| Settings        | `Голос` -> распознавание речи, локальный Whisper, размер модели, поля провайдера и языка                                                                                                                                | Подписи русские, старые `Local (Whisper)`, `tiny/base/...` как пользовательский текст и `STT`-названия не видны   | FIXED    |
| Providers       | `Сохранить` на экране модели                                                                                                                                                                                            | Не менять реальный provider id на `custom`, если модель работает через DeepSeek или другой каталоговый провайдер | FIXED    |
| Providers       | `Добавить провайдера` -> `OpenAI Codex`                                                                                                                                                                                 | Показывать только CLI-вход, без ложного OAuth                                                                    | FIXED    |
| Providers       | `OpenAI Codex` -> `CLI-вход` -> `Перейти к проверке`                                                                                                                                                                    | Перевести пользователя к проверке CLI-входа без OAuth и английских ошибок                                        | FIXED    |
| Providers       | `Удалить` у системных, локальных, CLI и активных провайдеров                                                                                                                                                            | Не выглядеть рабочей кнопкой, если удалять нечего или опасно                                                     | FIXED    |
| Providers       | `Удалить` у неактивного API-провайдера                                                                                                                                                                                  | Удалить ключ/профиль через `/api/hermes-config`, без несуществующего `POST /api/claude-config`                   | FIXED    |
| Skills          | `Установить`, `Удалить`, toggle включения                                                                                                                                                                               | В managed/zero-fork режиме действия недоступны и не должны выглядеть рабочими                                    | FIXED    |
| MCP             | Переход `/mcp` из сайдбара                                                                                                                                                                                              | Если текущий Hermes Agent не поддерживает MCP, показать понятный disabled-экран без мёртвых кнопок               | DISABLED |
| MCP             | `Добавить сервер`, `Установить`                                                                                                                                                                                         | Не быть активными при `mcp=false` и `mcpFallback=false`                                                          | FIXED    |
| Память          | `База знаний` -> `Граф связей`, `Настройки`, `Локальная папка`, `GitHub-репозиторий`                                                                                                                                     | Открывать нужные состояния без отката выбора, без английских fallback-подписей и без сетевых/browser ошибок     | FIXED    |
| Updates         | `Скрыть обновление Hermes Agent`                                                                                                                                                                                        | Убрать карточку обновления                                                                                       | OK       |
| Updates         | `Обновить COMANDOS AI Workspace`                                                                                                                                                                                        | Обновить панель до новой версии без мёртвой кнопки и без сырого технического текста для пользователя             | FIXED    |
| HermesWorld     | `Запустить в панели`, `Продолжить без изменений`                                                                                                                                                                        | Запустить локальный `PlaygroundScreen` и снять предупреждение без внешнего iframe и без ошибок                    | FIXED    |
| HermesWorld     | `Инвентарь`, `Журнал заданий`, `Карта`, `Настройки`, `Режим фокуса`                                                                                                                                                      | Правый rail остаётся кликабельным даже при открытой боковой панели                                               | FIXED    |
| HermesWorld     | `Сделать снимок мира`, `Открыть полноэкранный режим`, `Копировать ссылку`, `Повторить озвучку мира`, `Выключить озвучку`, `Выключить звук`, `Настроить аватар`                                                          | Нижний utility dock остаётся кликабельным, а emoji-кнопки имеют русские имена для проверки                      | FIXED    |

## Автоматические проверки

| Команда                                                                                                                                                                                                                                                         | Результат | Примечание                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `git diff --check`                                                                                                                                                                                                                                              | OK        | Пробельных ошибок нет                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `pnpm exec vitest run src/server/license.test.ts src/server/update-system.test.ts src/server/claude-tasks-backend.test.ts src/server/kanban-backend.test.ts src/server/hermes-config-migration.test.ts src/components/settings-dialog/settings-dialog.test.ts`  | OK        | 23 теста прошли                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Есть только предупреждения Vite про размер чанков и смешанные dynamic/static imports                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm exec vitest run src/server/update-system.test.ts` после bump/cache fix                                                                                                                                                                                    | OK        | Проверка сравнения версий и настроек polling                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `pnpm exec vitest run src/components/update-center-notifier.test.ts src/server/update-system.test.ts`                                                                                                                                                           | OK        | 5 тестов прошли; проверены временное скрытие обновлений и сравнение версий                                                                                                                                                                                                                                                                                                                                                                                          |
| `pnpm exec vitest run src/components/agent-view/agent-usage-helpers.test.ts src/components/update-center-notifier.test.ts src/server/update-system.test.ts`                                                                                                     | OK        | 12 тестов прошли; проверены выбор провайдера расхода, формат версий и managed release notes                                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm exec vitest run src/routes/api/-terminal-session-lifecycle.test.ts src/components/settings-dialog/settings-dialog.test.ts src/components/update-center-notifier.test.ts src/server/update-system.test.ts`                                                 | OK        | 14 тестов прошли; проверены stale terminal API, настройки, обновления и формат версий                                                                                                                                                                                                                                                                                                                                                                               |
| `pnpm exec vitest run src/routes/api/-terminal-session-lifecycle.test.ts`                                                                                                                                                                                       | OK        | 4 теста прошли; проверены stale resize/input и cleanup listeners при terminal stream close/abort                                                                                                                                                                                                                                                                                                                                                                    |
| `pnpm exec eslint src/components/agent-view/agent-usage-helpers.ts src/components/agent-view/agent-usage-helpers.test.ts src/components/update-center-notifier.tsx src/components/update-center-notifier.test.ts src/server/update-system.test.ts`              | OK        | Новая логика и тесты проходят; полный `agent-view-panel.tsx` был отдельным старым долгом и закрыт позже на `.90`                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm exec eslint src/routes/api/terminal-resize.ts src/routes/api/terminal-input.ts src/routes/api/-terminal-session-lifecycle.test.ts src/screens/settings/components/provider-wizard.tsx`                                                                    | OK        | Затронутые файлы проходят lint; есть только старое предупреждение про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                                               |
| Маршрутный Playwright-обход 17 страниц                                                                                                                                                                                                                          | OK        | Нет аварийных экранов, console errors, сетевых падений и проверяемых английских пользовательских фраз                                                                                                                                                                                                                                                                                                                                                               |
| Live Playwright: `/settings/providers` -> `OpenAI Codex` -> `CLI-вход` -> `Перейти к проверке` на `.17`                                                                                                                                                         | OK        | Нет `Start OAuth`, `OAuth device flow not supported`, `Key required`, старых английских подсказок, console errors и HTTP 4xx/5xx                                                                                                                                                                                                                                                                                                                                    |
| Live API: stale `/api/terminal-resize` и `/api/terminal-input` на `.17`                                                                                                                                                                                         | OK        | Оба endpoint вернули HTTP 200; resize `ok=true`, input `ok=false`, оба `attached=false`                                                                                                                                                                                                                                                                                                                                                                             |
| Live Playwright: `/terminal` -> 14 пунктов бокового меню на `.19`                                                                                                                                                                                               | OK        | Все клики сработали; пустых/аварийных экранов нет; terminal API не дал 4xx/5xx; browser console чистая; systemd-логи без новых terminal ошибок и `MaxListenersExceededWarning`                                                                                                                                                                                                                                                                                      |
| Live API + Playwright: `/skills` на `.20`                                                                                                                                                                                                                       | OK        | `/api/skills` отдаёт `actions`; direct install/toggle дают русские 501; UI показывает предупреждение и disabled-кнопки `Недоступно`/`Удаление недоступно`; активных install/uninstall кнопок, console errors и HTTP 4xx/5xx в UI-проходе нет                                                                                                                                                                                                                        |
| `pnpm exec vitest run src/server/stt-transcription.test.ts`                                                                                                                                                                                                     | OK        | 4 теста прошли; проверены STT-настройки и русская ошибка отсутствующего ключа Groq                                                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm exec eslint src/lib/voice-errors.ts src/hooks/use-voice-input.ts src/hooks/use-voice-recorder.ts src/screens/chat/components/chat-composer.tsx src/routes/api/transcribe.ts src/server/stt-transcription.ts src/server/stt-transcription.test.ts`         | OK        | Ошибок нет; остались старые `no-shadow` warnings в `chat-composer.tsx`                                                                                                                                                                                                                                                                                                                                                                                              |
| `pnpm exec vitest run src/server/update-system.test.ts`                                                                                                                                                                                                         | OK        | Проверена нормализация release notes: устаревший переход `.19 -> .20` не должен всплывать, если реально доступно `.20 -> .22`                                                                                                                                                                                                                                                                                                                                       |
| `pnpm exec vitest run src/components/settings-dialog/settings-dialog.test.ts src/routes/api/-oauth-device-code.test.ts`                                                                                                                                         | OK        | 6 тестов прошли; проверены Codex CLI вместо OAuth и русское сообщение прямого OAuth API                                                                                                                                                                                                                                                                                                                                                                             |
| `pnpm exec eslint src/components/settings-dialog/settings-dialog.tsx src/components/settings-dialog/settings-dialog.test.ts src/routes/api/oauth.device-code.ts src/routes/api/-oauth-device-code.test.ts`                                                      | OK        | Настройки провайдеров и OAuth endpoint проходят lint; есть только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm exec vitest run src/routes/api/-hermes-config.test.ts src/screens/settings/providers-screen.test.ts`                                                                                                                                                      | OK        | 13 тестов прошли; проверены запрет удаления активного/Codex/системного провайдера и удаление неактивного API-провайдера                                                                                                                                                                                                                                                                                                                                             |
| `pnpm exec eslint src/server/hermes-config-store.ts src/server/hermes-config-route.ts src/routes/api/-hermes-config.test.ts src/screens/settings/providers-screen.tsx src/screens/settings/providers-screen.test.ts`                                            | OK        | Сервер удаления провайдера и экран провайдеров проходят lint; есть только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                          |
| `pnpm exec vitest run src/routes/api/-history.test.ts src/server/portable-history.test.ts src/lib/send-stream-session-headers.test.ts src/screens/chat/hooks/use-streaming-message.test.ts`                                                                     | OK        | 11 тестов прошли; проверены local portable history, заголовки resolved session и streaming hook                                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm exec eslint src/screens/chat/hooks/use-chat-history.ts src/routes/api/history.ts src/routes/api/-history.test.ts`                                                                                                                                         | OK        | Исправленная история чата и API history проходят lint; есть только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                 |
| Live Playwright: `/settings/providers` -> `Добавить провайдера` -> `OpenAI Codex` -> `CLI-вход` на `.23`                                                                                                                                                        | OK        | Codex CLI-текст виден; `Start OAuth`, `OAuth device flow not supported`, `Key set`, `Key required` не найдены; console/network чистые; прямой `/api/oauth/device-code` для Codex вернул русское объяснение с `codex login`                                                                                                                                                                                                                                          |
| Live Playwright/API: `/settings/providers` на `.24`                                                                                                                                                                                                             | OK        | DeepSeek как активный провайдер и Hermes как системный провайдер показывают disabled `Удалить` с русской причиной; прямой `/api/hermes-config` защищает active/Codex/unknown; console errors и сетевых 4xx/5xx в UI нет                                                                                                                                                                                                                                             |
| Live Playwright/API/logs: `/jobs` на `.24`                                                                                                                                                                                                                      | OK        | Создано временное cron-задание, проверены история, пауза, возобновление, редактирование, ручной запуск и удаление; все `/api/claude-jobs` ответы 200, output появился, browser console чистая, свежие `comandos-workspace`/`hermes-gateway` логи без ошибок, тестовых задач не осталось                                                                                                                                                                             |
| Live Playwright/API/logs: `/chat/new` первая отправка на `.28`                                                                                                                                                                                                  | OK        | UI показал `user + assistant`, `/api/history` вернул local history с обеими ролями, browser console и network чистые, свежие `comandos-workspace`/`hermes-gateway` логи без ошибок, QA-сессия удалена                                                                                                                                                                                                                                                               |
| Live API: `/api/update/workspace` на `clawd` `.20 -> .22`                                                                                                                                                                                                       | OK        | Обновление применилось; release notes вернули актуальный переход `.20 -> .22`; `comandos-workspace.service` active; повторный `/api/update/status` показывает Workspace `.22`, `updateAvailable=false`                                                                                                                                                                                                                                                              |
| Чистая установка из GitHub: `install.sh` -> заполненный env -> `deploy.sh` на QA-контур `.36`                                                                                                                                                                   | OK        | `/tmp/hermes-clean-install-qa-20260605105010`; `qa.194.113.38.109.nip.io` отвечает 200; Caddy содержит отдельные блоки `COMANDOS HERMES` и `COMANDOS HERMES QA`; порты `3030/8642` и `3130/8742` слушают localhost; QA user services active; main live отвечает 200                                                                                                                                                                                                 |
| QA Playwright: экран пароля -> экран лицензии на `.36`                                                                                                                                                                                                          | FIXED     | Найдены фоновые `401 /api/connection-status` и `500 /api/claude-config`: рабочие проверки монтировались до завершения входа/лицензии. В `.37` root layout ждёт auth/license status и не запускает workspace/onboarding API заранее                                                                                                                                                                                                                                  |
| QA Playwright: кнопка `Активировать` с неверным ключом на `.37`                                                                                                                                                                                                 | FIXED     | Пользователь видел русский отказ, но browser console получала ожидаемый `401 /api/license/activate`. В `.38` управляемый отказ активации возвращает HTTP 200 с `ok:false`, чтобы кнопка не оставляла техническую ошибку в журнале                                                                                                                                                                                                                                   |
| QA Playwright/logs: пароль -> лицензия -> неверный ключ на `.38`                                                                                                                                                                                                | OK        | Видимый текст: `лицензия не найдена или отключена`; browser console, failed requests и HTTP 4xx/5xx пустые; свежие `comandos-workspace`/`hermes-gateway` логи без ошибок; QA и основной домен отвечают 200                                                                                                                                                                                                                                                          |
| QA Playwright: кнопка `Активировать` с временным QA-ключом на `.38`                                                                                                                                                                                             | OK        | Кнопка провела внутрь `/chat/new`, browser console/network пустые; после возврата исходного license server URL и удаления временного dev-ключа сохранённая QA-активация продолжает пускать в панель                                                                                                                                                                                                                                                                 |
| QA Playwright/API/logs: первый чат на `.38`                                                                                                                                                                                                                     | OK        | Через UI отправлены два коротких сообщения в `main`; `/api/history?sessionKey=main` вернул пары `user + assistant` (`готово`, `принято`); browser console и HTTP 4xx/5xx пустые; в gateway есть один warning Hermes Agent про `Stored system prompt ... is null`, пользовательского сбоя нет                                                                                                                                                                        |
| QA Playwright/API: кнопка `Новая сессия` на `.38`                                                                                                                                                                                                               | OK        | Клик ведёт на `/chat/new`; `/api/history?sessionKey=new` возвращает пустую историю; composer появляется после догрузки экрана; browser console и HTTP 4xx/5xx пустые. Отменённые `ERR_ABORTED` на route-change относятся к остановке старых запросов `main`                                                                                                                                                                                                         |
| QA Playwright: пункт `HermesWorld` на `.38`                                                                                                                                                                                                                     | PARTIAL   | Экран открывался, но browser console показывала autoplay warning `AudioContext was not allowed to start`. В `.39` игровые звуки и ambient были отложены до пользовательского жеста, но QA показала, что общий модуль звуков панели всё ещё создаёт Web Audio заранее                                                                                                                                                                                                |
| QA Playwright: пункт `HermesWorld` на `.39`                                                                                                                                                                                                                     | PARTIAL   | Найдены 2 оставшихся autoplay warning из общего `src/lib/sounds.ts`. В `.40` общий звук панели агента/чата тоже ждёт пользовательский жест, но QA показала, что текущий `/playground` грузит внешний standalone iframe `hermes-world.ai`, который создаёт свой `AudioContext`                                                                                                                                                                                       |
| QA Playwright: пункт `HermesWorld` на `.40`                                                                                                                                                                                                                     | FIXED     | В `.41` `/playground` больше не автозагружает внешний iframe: сначала показывает русский экран запуска `HermesWorld готов к запуску`, а встроенный запуск начинается только после клика `Запустить в панели` и с iframe permission `autoplay`                                                                                                                                                                                                                       |
| QA Playwright/logs: пункт `HermesWorld` на `.41`                                                                                                                                                                                                                | OK        | До клика iframe не грузится (`iframeCount=0`), console и HTTP 4xx/5xx пустые; после `Запустить в панели` iframe появляется (`iframeCount=1`), browser console/HTTP/failed requests пустые; свежие `comandos-workspace`/`hermes-gateway` логи без ошибок                                                                                                                                                                                                             |
| QA Playwright/API/logs: `Добавить провайдера` -> `OpenAI Codex` на `.43`                                                                                                                                                                                        | OK        | В мастере для Codex виден только `CLI-вход`; `Start OAuth`, `OAuth device flow not supported` и инструкция API-ключа для Codex не появляются; `Перейти к проверке` показывает `~/.codex/auth.json`; прямой `/api/oauth/device-code` возвращает HTTP 200 `ok:false` с русским объяснением; browser console и HTTP 4xx/5xx пустые; свежие логи без ошибок                                                                                                             |
| QA Playwright/logs: модалка `Настройки` из панели на `.43`                                                                                                                                                                                                      | OK        | Шестерёнка открывает старую модалку настроек; конфигурация догружается, API-запросы настроек возвращают 200; `Start OAuth`, `OAuth device flow not supported`, `Key set`, `Key required` не найдены; browser console/HTTP 4xx/5xx и свежие systemd-логи пустые                                                                                                                                                                                                      |
| `pnpm exec eslint src/screens/mcp/mcp-screen.tsx`                                                                                                                                                                                                               | OK        | MCP-экран после отключения действий в `off`-режиме проходит lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                  |
| `pnpm exec vitest run src/screens/mcp/-marketplace-install-confirmation.test.tsx src/routes/api/-mcp.test.ts src/routes/api/-mcp-logs.test.ts`                                                                                                                  | OK        | 32 теста прошли; проверены API-контракты MCP и install confirmation                                                                                                                                                                                                                                                                                                                                                                                                 |
| QA API update: `.43 -> .44`                                                                                                                                                                                                                                     | OK        | QA-контур обновился через `/api/update/workspace`; `/opt/comandos/hermes-qa/workspace/package.json` показывает `2.3.0-comandos.44`; сервисы active                                                                                                                                                                                                                                                                                                                  |
| QA Playwright/API/logs: `/mcp` на `.44`                                                                                                                                                                                                                         | OK        | После входа `/mcp` показывает русское disabled-состояние; `/api/gateway-status` HTTP 200, gateway available, `mcp=false`, `mcpFallback=false`; `/api/mcp` HTTP 200 `capability_unavailable`; browser console/network и свежие workspace/gateway логи пустые                                                                                                                                                                                                         |
| QA Playwright/logs: сайдбарный обход 17 маршрутов на `.44`                                                                                                                                                                                                      | OK        | `/dashboard`, `/chat/new`, `/chat/main`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground`: fatal-текста нет, browser console пустая, HTTP 4xx/5xx и failed requests пустые, свежие workspace/gateway логи без ошибок                                                                                                                   |
| QA Playwright/logs: вкладки `/settings` на `.44`                                                                                                                                                                                                                | OK        | Проверены `Подключение`, `Модель и провайдер`, `Поведение агента`, `Голос`, `Экран`, `Тема`, `Чат`, `Сигналы` (`section=notifications`) и `Язык`; все открываются без console/network ошибок, свежие workspace/gateway логи пустые                                                                                                                                                                                                                                  |
| QA Playwright/logs: нижняя шестерёнка `Настройки` на `.44`                                                                                                                                                                                                      | OK        | Кнопка из сайдбара открывает поверхность настроек; видны `Настройки` и блок модели/провайдера, старые `Start OAuth`, `OAuth device flow not supported`, `Key required`, `Key set` не появляются; browser console/network и свежие логи пустые                                                                                                                                                                                                                       |
| `pnpm exec eslint src/components/file-explorer/file-explorer-sidebar.tsx`                                                                                                                                                                                       | OK        | Исправление файлового контекстного меню проходит lint; есть только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                 |
| `pnpm exec vitest run src/screens/mcp/-marketplace-install-confirmation.test.tsx src/routes/api/-mcp.test.ts src/routes/api/-mcp-logs.test.ts`                                                                                                                  | OK        | 32 теста прошли после `.48`; проверочные MCP/API тесты не сломаны                                                                                                                                                                                                                                                                                                                                                                                                   |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.48` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA API update: `.44/.47 -> .48`                                                                                                                                                                                                                                 | OK        | QA-контур обновлён до `2.3.0-comandos.48`; `zero_server_js=0`, `node --check` чистый, `/files` локально и через `https://qa.194.113.38.109.nip.io/files` отдаёт 200; автоматический backup очищен после проверки из-за малого свободного места                                                                                                                                                                                                                      |
| QA Playwright/API/logs: `/files` context menu на `.48`                                                                                                                                                                                                          | OK        | Реальный правый клик по файлу открыл меню; `Удалить` показал confirm, после принятия файл исчез из UI и `/api/files`; по самому действию browser console/page errors/failed requests/HTTP 4xx/5xx = 0; свежие workspace/gateway логи без ошибок; тестовые файлы удалены                                                                                                                                                                                             |
| `pnpm exec vitest run src/components/update-center-notifier.test.ts src/server/update-system.test.ts`                                                                                                                                                           | OK        | 15 тестов прошли после `.49`; проверены русская нормализация сетевой ошибки, JSON parse error, русская backend-ошибка без перезаписи и скрытие сырой английской backend-ошибки                                                                                                                                                                                                                                                                                      |
| `pnpm exec eslint src/components/update-center-notifier.tsx src/components/update-center-notifier.test.ts src/server/update-system.ts src/server/update-system.test.ts`                                                                                         | OK        | Код центра обновлений проходит lint; есть только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                                   |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.49` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA Playwright/API/logs: update center `.48 -> .49`                                                                                                                                                                                                              | OK        | До обновления `/api/update/status` видел `.48 -> .49`, карточка обновления и `Скрыть на 24 часа` работали; клик `Обновить` вызвал `/api/update/workspace` 200 и довёл QA до `.49`; после рестарта status `current`, карточки и сырого error-текста нет, user services active, свежие workspace/gateway логи без ошибок; автоматический backup очищен после проверки                                                                                                 |
| `pnpm exec vitest run src/screens/settings/providers-screen.test.ts src/components/update-center-notifier.test.ts src/server/update-system.test.ts`                                                                                                             | OK        | 21 тест прошёл после `.50`; добавлен regression-тест, что `deepseek` и неизвестные provider id не превращаются в `custom`                                                                                                                                                                                                                                                                                                                                           |
| `pnpm exec eslint src/screens/settings/providers-screen.tsx src/screens/settings/providers-screen.test.ts src/components/update-center-notifier.tsx src/components/update-center-notifier.test.ts src/server/update-system.ts src/server/update-system.test.ts` | OK        | Экран провайдеров, update UI и update system проходят lint; остались старые предупреждения pnpm/ESLint без ошибок                                                                                                                                                                                                                                                                                                                                                   |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.50` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA Playwright/API/logs: `/settings/providers` на `.50`                                                                                                                                                                                                          | OK        | QA обновлена кнопкой `Обновить` до `.50`; `Сохранить` сохранил `activeProvider=deepseek`, `activeModel=deepseek-chat`; Codex wizard дошёл до `CLI-вход` и `Перейти к проверке`; старые `Start OAuth`, `OAuth device flow not supported`, `Key required`, `Key set` не найдены; browser console/network и свежие workspace/gateway логи без ошибок; автоматический backup очищен                                                                                     |
| `pnpm exec eslint src/screens/chat/components/sidebar/session-item.tsx`                                                                                                                                                                                         | OK        | Меню сессии после русификации проходит lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                       |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.51` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA Playwright/logs: меню `Настройки сессии` на `.51`                                                                                                                                                                                                            | OK        | QA обновлена до `.51`; меню сессии показывает `Закрепить сессию`/`Открепить сессию`, `Pin session`/`Unpin session` не найдены; browser console/network и свежие workspace/gateway логи без ошибок; автоматический backup очищен                                                                                                                                                                                                                                     |
| QA Playwright: `Пользователь` и `Настройки чата` на `.51`                                                                                                                                                                                                       | OK        | `Пользователь` открывает menuitem `Настройки`, клик ведёт в `/settings`; `Настройки чата` открывает нижнее меню composer с русскими пунктами `по умолчанию`, `Рабочая папка`, `Низкое`, `deepseek-chat`; browser console и HTTP 4xx/5xx пустые                                                                                                                                                                                                                      |
| `pnpm exec vitest run src/server/swarm-roster.test.ts`                                                                                                                                                                                                          | OK        | 3 теста прошли после `.52`; добавлен regression-тест, что legacy-описание `builder` русифицируется, а кастомный текст другого агента сохраняется                                                                                                                                                                                                                                                                                                                    |
| `pnpm exec eslint src/server/swarm-roster.ts src/server/swarm-roster.test.ts`                                                                                                                                                                                   | OK        | Ростер роя проходит lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                                          |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.52` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA Playwright/API/logs: `/swarm` и `/swarm2` на `.52`                                                                                                                                                                                                           | OK        | QA обновлена кнопкой `Обновить` до `.52`; `/swarm` и `/swarm2` больше не показывают старые английские миссии из `swarm.yaml`, видны русские тексты `Делает ограниченные продуктовые...`, `Проверяет видимое пользователю поведение...`, `Делит миссии на безопасные...`; browser console и HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок; автоматический backup очищен                                                                              |
| QA Playwright/API/logs: `/profiles` на `.52`                                                                                                                                                                                                                    | OK        | Проверены кнопки профилей: `Активировать`, `Детали`, `Обновить`, `Сохранить`, `Закрыть`, `Переименовать`, `Удалить`, мастер `Создать профиль` и подтверждения удаления. Временный профиль создан, описание сохранено, профиль переименован, активирован, `default` восстановлен, тестовые профили удалены; `/api/profiles/list` в конце показывает только активный `default`; browser console, HTTP 4xx/5xx, failed requests и свежие workspace/gateway логи пустые |
| `pnpm exec vitest run src/server/claude-tasks-backend.test.ts`                                                                                                                                                                                                  | OK        | 6 тестов прошли после `.53`; добавлены проверки, что `priority`, `tags` и `due_date` сохраняются в kanban `spec` и читаются обратно без показа служебного блока в описании                                                                                                                                                                                                                                                                                          |
| `pnpm exec eslint src/server/claude-tasks-backend.ts src/server/claude-tasks-backend.test.ts`                                                                                                                                                                   | OK        | Ошибок lint нет; остались только старые предупреждения тестовых mock-функций без `await` и `.eslintignore`                                                                                                                                                                                                                                                                                                                                                          |
| `pnpm build`                                                                                                                                                                                                                                                    | OK        | Production build `.53` прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports                                                                                                                                                                                                                                                                                                                                                 |
| QA update button: `.52 -> .53`                                                                                                                                                                                                                                  | OK        | QA обновлена настоящим кликом по карточке `Доступно обновление COMANDOS AI Workspace`; `/api/update/workspace` вернул 200, пакет на диске `.53`, JS integrity OK, user-сервисы active, свежие логи без ошибок; автоматический backup очищен после проверки                                                                                                                                                                                                          |
| QA Playwright/API/logs: `/tasks` на `.53`                                                                                                                                                                                                                       | OK        | Проверены `Показать готовые`/`Скрыть готовые`, `Обновить`, кнопки `Добавить в` для всех колонок включая `Готово`, `Новая задача`, `Создать`, `Сохранить`, `Удалить` и confirm. Временная задача создана с `priority=high`, тегами и сроком, обновлена до `priority=low` с новыми тегами/сроком, удалена; browser console, HTTP 4xx/5xx, failed requests и свежие workspace/gateway логи пустые                                                                      |
| `pnpm exec vitest run src/components/update-center-notifier.test.ts src/server/update-system.test.ts src/server/hermes-cron-profiles.test.ts`                                                                                                                   | OK        | 21 тест прошёл после `.60`; проверены update UI/system и запуск cron с accepted hooks                                                                                                                                                                                                                                                                                                                                                                               |
| `pnpm exec eslint src/components/update-center-notifier.tsx src/components/ui/toast.tsx src/server/hermes-cron-profiles.ts src/server/hermes-cron-profiles.test.ts`                                                                                             | OK        | Затронутые update/jobs файлы проходят lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                        |
| `pnpm build` после `.60`                                                                                                                                                                                                                                        | OK        | Production build прошёл; `dist/server` не содержит пустых JS, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                                                                                                                |
| QA update/manual install: `.59 -> .60`                                                                                                                                                                                                                          | OK        | QA обновлена штатным `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; пакет на диске `.60`, `zero_server_js=0`, `node --check` чистый, user-сервисы active, свежие workspace/gateway логи без ошибок                                                                                                                                                                                                                                                 |
| QA Playwright/API/logs: `/jobs` на `.60`                                                                                                                                                                                                                        | OK        | Проверены `Обновить список`, создание и отмена формы, создание задания, редактирование названия/расписания/навыков, `Пауза`, `Возобновить`, `Запустить сейчас`, раскрытие/скрытие истории, поиск, `Удалить` с confirm. Browser console/page errors/failed requests/HTTP 4xx/5xx пустые, `last_run_at` обновился, тестовых заданий не осталось, свежие workspace/gateway логи без ошибок                                                                             |
| `pnpm exec vitest run src/server/update-system.test.ts src/server/comandos-version-lock.test.ts src/components/update-center-notifier.test.ts`                                                                                                                  | OK        | 18 тестов прошли после `.61`; проверены fallback на cached manifest, синхрон package/manifest/lock и update UI                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm exec eslint src/server/update-system.ts src/server/update-system.test.ts src/server/comandos-version-lock.test.ts src/components/update-center-notifier.tsx`                                                                                              | OK        | Update system и связанный UI проходят lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                        |
| `pnpm build` после `.61`                                                                                                                                                                                                                                        | OK        | Production build прошёл; `dist/server` не содержит пустых JS, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                                                                                                                |
| QA update/manual install: `.60/.61 state .59 -> .61`                                                                                                                                                                                                            | OK        | Первый прогон выявил старый `comandos-hermes.lock=.59`; после lock-фикса повторный прогон создал `.runtime/update-manifest-cache.json`, записал installed-state `.61`, `/api/update/status` показывает Workspace `managed/current`, user-сервисы active, свежие workspace/gateway логи без ошибок                                                                                                                                                                   |
| `pnpm exec vitest run src/routes/api/-hermes-config.test.ts`                                                                                                                                                                                                    | OK        | 13 тестов прошли после `.63`; проверены быстрый `/api/hermes-config`, JSON `/api/config-get`, сохранение dotted `path/value` и `raw`-патчей для мастера провайдеров                                                                                                                                                                                                                                                                                                 |
| `pnpm exec eslint src/server/hermes-config-route.ts src/routes/api/config-get.ts src/routes/api/config-patch.ts src/routes/api/-hermes-config.test.ts`                                                                                                          | OK        | Затронутые маршруты конфигурации проходят lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                    |
| `pnpm build` после `.63`                                                                                                                                                                                                                                        | OK        | Production build прошёл; route tree подхватил `/api/config-get` и `/api/config-patch`, `dist/server` не содержит пустых JS, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                                                  |
| `pnpm exec vitest run src/server/update-system.test.ts`                                                                                                                                                                                                         | OK        | 10 тестов прошли после `.66`; regression-тест проверяет cached managed status, отсутствие git-проверки Hermes Agent в managed-режиме и отсутствие ложного agent update при совпавшем ref и пустой строке версии                                                                                                                                                                                                                                                       |
| `pnpm exec eslint src/server/update-system.ts src/server/update-system.test.ts`                                                                                                                                                                                  | OK        | Update status и regression-тест проходят lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                                                                      |
| QA API/logs: update status на `.66`                                                                                                                                                                                                                              | OK        | `/api/update/status` HTTP 200 примерно за 1 сек.; Workspace и Hermes Agent `updateAvailable=false`; `/api/hermes-config`, `/api/config-get`, `/api/models` HTTP 200; свежие workspace/gateway логи после повторной проверки без ошибок                                                                                                                                                                                                                               |
| QA Playwright/logs: 14 основных пунктов меню на `.66`                                                                                                                                                                                                            | OK        | `/chat/new`, `/dashboard`, `/chat`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/memory`, `/skills`, `/mcp`, `/profiles` открываются без экрана входа, browser console пустая, HTTP 4xx/5xx пустые                                                                                                                                                                                                                             |
| QA Playwright/logs: `Добавить провайдера` -> `OpenAI Codex` -> `CLI-вход` на `.66`                                                                                                                                                                                | OK        | В мастере виден `codex login`; старые `Start OAuth` и `OAuth device flow not supported` не появляются; `Открыть терминал` создаёт новую вкладку `/terminal` и переводит мастер на шаг проверки; свежая повторная проверка browser/API/logs без ошибок                                                                                                                                                                                                                  |
| QA Playwright/API/logs: `/settings?section=connection` на `.67`                                                                                                                                                                                                  | OK        | Проверены `Сохранить и проверить`, `Сбросить` и invalid URL. UI показывает фактический результат `/api/connection-status`, источник после сброса снова `env`, английских ошибок нет, browser console/failed requests/HTTP 4xx в пользовательском клике и свежие workspace/gateway логи пустые                                                                                                                                                                        |
| QA Playwright/API/logs: серверные вкладки настроек на `.67`                                                                                                                                                                                                      | OK        | Проверены `Поведение агента`, `Экран`, `Голос`: `max_turns`, `gateway_timeout`, `tool_use_enforcement`, `display.personality`, `streaming`, `show_reasoning`, `show_cost`, `compact`, `tts.provider`, `tts.edge.voice`, `stt.enabled`, `stt.provider`, `stt.local.model_size`. Каждый control менялся через UI, подтверждался через `/api/config-get`, возвращался через `/api/config-patch`; browser/network чистые, свежие workspace/gateway логи пустые             |
| QA Playwright/API/logs: безопасные действия `/settings?section=claude` на `.67`                                                                                                                                                                                   | OK        | Проверены `Показать/Скрыть запасную модель`, открытие/отмена редактирования API-ключа, переключатель памяти отдельным тестом, таймаут терминала с возвратом, добавление и удаление временного custom provider. Конфиг после проверки восстановлен, временных provider-строк нет                                                                                                                                                                                      |
| QA Playwright: локальные вкладки настроек на `.67`                                                                                                                                                                                                                | PARTIAL   | `Тема`, `Чат`, `Сигналы` прошли: theme button, 6 chat-switches, chat width, 3 notification switches, threshold range и 2 model selects меняются и возвращаются без browser/network ошибок. `Язык` воспроизвёл дефект: активный select показывает 11 языков, хотя локаль всегда остаётся `ru`; исправлено в `.68`                                                                                                                                                     |
| QA Playwright/logs: `/settings?section=language` на `.68`                                                                                                                                                                                                          | OK        | После обновления QA до `2.3.0-comandos.68` вкладка языка показывает отключённый select с единственным option `Русский`, видны русские объяснения про отключённые переводы; browser console, failed requests, HTTP 4xx/5xx и свежие workspace/gateway логи пустые                                                                                                                                       |
| QA Playwright/logs: `/conductor` безопасные действия на `.68`                                                                                                                                                                                                      | OK        | Проверены `Как это работает`, `Новая миссия` без текста и `Открыть настройки оркестратора`: справка открывается/закрывается, новая миссия открывает форму с disabled `Запустить миссию`, настройки открываются; browser console, page errors, failed requests и HTTP 4xx/5xx пустые                                                                                                                       |
| QA Playwright/API/logs: `/operations` на `.68`                                                                                                                                                                                                                     | OK        | Проверены `Результаты`, `Настройки`, `Новый агент`, `Создать агента`, `Открыть настройки для <temp>`, `Удалить агента`. Пустое имя блокирует создание; временный профиль `qa-ops-*` создан через UI, открыт в настройках и удалён через UI; `/api/profiles/list` в конце не содержит `qa-ops-*`; browser console/page errors/HTTP 4xx/5xx пустые, route-change `/api/events` abort ожидаем и не влияет на пользователя |
| QA Playwright/logs: `/swarm` безопасные действия на `.68`                                                                                                                                                                                                          | OK        | Проверены `Как это работает`, `Уведомления роя`, вкладки `Управление`/`Доска`/`Входящие`/`Работа`, `МАРШРУТ`, `Добавить агента`. Справка и уведомления открываются, вкладки переключаются, маршрутная панель открывается, форма добавления открывается и предзаполняет `Builder`, `swarm13`, `Swarm13`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые |
| QA Playwright/logs: `/skills` на `.68`                                                                                                                                                                                                                             | OK        | Проверены вкладки `Установленные`/`Каталог`, поиск по каталогу, диалог `Детали`, disabled-действие `Недоступно`, состояние пагинации и empty-state поиска. Установку/удаление навыков не запускали, чтобы не менять набор навыков; browser console/page errors/failed requests/HTTP 4xx/5xx пустые                                                                                                      |
| QA Playwright/logs: `/memory` на `.68`                                                                                                                                                                                                                             | OK        | Проверены вкладки `Память`/`База знаний` и поиск по файлам памяти без записи новых фактов. Экран показывает `ФАЙЛЫ ПАМЯТИ (0)` и пустое состояние `В memory/ или memories/ пока нет файлов`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые                                                                                                                                           |
| QA Playwright/logs: dashboard и пустой чат на `.68`                                                                                                                                                                                                                | OK        | Проверены `Настроить панель`/`Завершить настройку панели`, карточная ссылка `ОТКРЫТЬ ЧАТ →` ведёт в `/chat/main`, быстрые подсказки `Аудит рабочей папки`, `Записать правило в память`, `Создать файл` заполняют composer без отправки; после очистки `Отправить сообщение` снова disabled; browser console/page errors/HTTP 4xx/5xx пустые |
| `pnpm exec vitest run src/routes/api/-auth.test.ts src/server/comandos-version-lock.test.ts src/components/update-center-notifier.test.ts`                                                                                                                          | OK        | 11 тестов прошли после `.69`; новый regression-тест проверяет, что неверный пароль возвращает `Неверный пароль.` с HTTP 200, а успешный вход по-прежнему ставит auth cookie                                                                                                                                                                                                                            |
| `pnpm exec eslint src/routes/api/auth.ts src/routes/api/-auth.test.ts`                                                                                                                                                                                             | OK        | Auth route и новый тест проходят lint; осталось только старое предупреждение ESLint про `.eslintignore`                                                                                                                                                                                                                                                                                                |
| `pnpm build` после `.69`                                                                                                                                                                                                                                           | OK        | Production build прошёл; `dist/server` не содержит пустых JS, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                                                  |
| QA update/manual install: `.68 -> .69`                                                                                                                                                                                                                             | OK        | QA обновлена штатным `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; пакет на диске `.69`, `zero_server_js=0`, `node --check` чистый, user-сервисы active, свежие workspace/gateway логи без ошибок                                                                                                                                                                                     |
| QA Playwright/API/logs: экран входа и реальная лицензия на `.69`                                                                                                                                                                                                    | OK        | Свежий браузер без cookie показал `Вход в Workspace`; пустая форма держит кнопку `Продолжить` disabled; один неверный пароль показал `Неверный пароль.` без browser console, failed requests и HTTP 4xx/5xx; реальный пароль из серверного `.env` открыл `/chat/new`; `/api/auth-check` вернул `authenticated=true`, `licenseActivated=true`, `backendReachable=true`; свежие workspace/gateway логи без ошибок |
| `pnpm exec vitest run src/server/stt-transcription.test.ts src/lib/stt-config.test.ts src/routes/api/-auth.test.ts src/server/comandos-version-lock.test.ts src/components/update-center-notifier.test.ts`                                                          | OK        | 17 тестов прошли после `.70`; проверены STT-настройки, серверная расшифровка, вход, синхрон версий и update UI                                                                                                                                                                                                                                                                                       |
| `pnpm exec eslint src/hooks/use-voice-input.ts src/lib/voice-errors.ts src/screens/chat/components/chat-composer.tsx src/routes/api/auth.ts src/routes/api/-auth.test.ts`                                                                                           | OK        | Ошибок нет; в `chat-composer.tsx` остались старые `no-shadow` warnings, уже вынесенные в отдельный долг                                                                                                                                                                                                                                                                                               |
| `pnpm build` после `.70`                                                                                                                                                                                                                                           | OK        | Production build прошёл; `dist/server` не содержит пустых JS, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                                                  |
| QA update/manual install: `.69 -> .70`                                                                                                                                                                                                                             | OK        | QA обновлена штатным `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; пакет на диске `.70`, `zero_server_js=0`, `node --check` чистый, user-сервисы active                                                                                                                                                                                                                              |
| QA Playwright/logs: `Голосовой ввод` на `.70`                                                                                                                                                                                                                       | OK        | Свежий браузер открыл `/chat/new`, клик по кнопке `Голосовой ввод` показал русскую ошибку `Не удалось распознать голос. Попробуйте ещё раз.`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок                                                                                                                                                 |
| `pnpm exec vitest run src/server/comandos-version-lock.test.ts src/components/update-center-notifier.test.ts src/components/workspace-shell.test.ts`                                                                                                                  | OK        | 12 тестов прошли после `.71`; проверены синхрон версий, update UI и shell-регрессии                                                                                                                                                                                                                                                                                                                     |
| `pnpm build` после `.71`                                                                                                                                                                                                                                             | OK        | Production build прошёл; корень `dist/server` не содержит лишних JS рядом с `server.js`, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                          |
| QA update/manual install: `.70 -> .71`                                                                                                                                                                                                                               | OK        | QA обновлена штатным `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; пакет на диске `.71`, `zero_server_js=0`, `node --check` чистый, user-сервисы active; диск QA остаётся тесным: 96%, около 2.7G свободно                                                                                                                                                                               |
| QA Playwright/logs: кнопки `/chat/new` и правой панели на `.71`                                                                                                                                                                                                       | OK        | Нажаты `Добавить файл`, `Настройки чата`, `Открыть панель агента`, `Изменить аватар`, `Переименовать агента`, `Переключить панель инспектора`, вкладки инспектора `Активность`/`Артефакты`/`Файлы`/`Память`/`Навыки`/`MCP`/`Логи`, `Закрыть инспектор`, `Скрыть панель агента`, `Переименовать сессию`, `Новая сессия`. Browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec vitest run src/server/comandos-version-lock.test.ts src/components/update-center-notifier.test.ts src/components/workspace-shell.test.ts`                                                                                                                  | OK        | 12 тестов прошли после `.73`; проверены синхрон версий, update UI и shell-регрессии                                                                                                                                                                                                                                                                                                                     |
| `pnpm build` после `.73`                                                                                                                                                                                                                                             | OK        | Production build прошёл; корень `dist/server` не содержит лишних JS рядом с `server.js`, `node --check` по server bundle чистый                                                                                                                                                                                                                                                                          |
| QA update/manual install: `.71 -> .73`                                                                                                                                                                                                                               | OK        | QA обновлена штатным `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; пакет на диске `.73`, `zero_server_js=0`, `node --check` чистый, user-сервисы active; диск QA остаётся тесным: 96%, около 2.5G свободно                                                                                                                                                                               |
| QA Playwright/logs: файловая панель `/chat/new` на `.73`                                                                                                                                                                                                              | OK        | Проверены `Показать файлы`, `Обновить`, `Новый файл`, `Сохранить`, `Загрузить`, контекстное меню файла, `Скачать`, `Удалить` и confirm. Временные файлы `qa-panel-new-*` и `qa-panel-upload-*` созданы, скачаны/удалены через UI и отсутствуют в `/api/files`; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/logs: OpenAI Codex CLI wizard на `.73`                                                                                                                                                                                                                  | OK        | Проверены `Добавить провайдера`, карточка `OpenAI Codex`, `CLI-вход`, видимая команда `codex login`, `Перейти к проверке` и `Открыть терминал`. Старые `Start OAuth`/`OAuth device flow not supported` не появляются; `Открыть терминал` создаёт новую вкладку `/terminal`; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/API/logs: `/settings` модель, API-ключи и custom providers на `.73`                                                                                                                                                                                      | OK        | Перед прогоном сделан backup `~/.hermes/config.yaml`; проверены `Показать/Скрыть запасную модель`, `Сохранить модель` без смены active provider/model, открытие и отмена 3 редакторов API-ключей без записи секретов, validation `Добавить в список своих провайдеров`, `Предложить из модели и URL`, `Заполнить из блока модели выше`, создание/применение/удаление временного `qa-audit-provider-*`. В конфиге хвостов нет; browser console/page errors/HTTP 4xx/5xx и workspace/gateway логи пустые |
| QA Playwright/API/logs: вкладки настроек `/settings` на `.73`                                                                                                                                                                                                           | OK        | Проверены `Память включена`, `Профиль пользователя`, терминальный `Таймаут`, `Максимум шагов`, `Таймаут gateway`, `Использование инструментов`, `Включить умный выбор`, `Дешёвая модель`, лимиты символов/слов, `Провайдер TTS`, `Включить STT`, `Провайдер STT`, `Стиль ответов`, `Потоковый вывод`, `Показывать рассуждения`, `Показывать стоимость`, `Компактный вид`, тема, 7 настроек чата, сигналы, умные подсказки и disabled `Язык интерфейса`. Серверный конфиг восстановлен точной копией backup, localStorage возвращён; browser console/page errors/HTTP 4xx/5xx и свежие workspace/gateway логи пустые |
| `pnpm build` после `.75`                                                                                                                                                                                                                                             | OK        | Production build прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports. Точечный ESLint по `conductor.tsx`/`swarm2-orchestrator-card.tsx` упирается в старые замечания по всему файлу, не связанные с удалением пустых обработчиков |
| QA Playwright/logs: `/conductor` офис оркестратора на `.75`                                                                                                                                                                                                            | FIXED     | На `.74` Playwright нашёл ложные кнопки `Агент 1`, `Агент 2`, `Агент 3`: `OfficeView` получал пустой `onViewOutput={() => {}}`. В `.75` пустые обработчики убраны в `/conductor` и карточке `/swarm2`; после обновления QA видимых кнопок стало 14, агентских ложных кнопок нет, `Изменить схему офиса` открывает `Сетка`/`Круглый стол`/`Штаб`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/components/file-explorer/file-explorer-sidebar.tsx`, `pnpm build` после `.76`                                                                                                                                                                    | OK        | `FileExplorerSidebar` проходит lint; version-lock test прошёл; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA Playwright/logs: скрытый toggle файловой панели на `.76`                                                                                                                                                                                                            | FIXED     | На `.75` общий сканер нашёл 1x1 видимую для Playwright кнопку `Показать или скрыть файловую панель` на `/chat/new` и `/files`. В `.76` удалён старый `sr-only` toggle из `FileExplorerSidebar`; нормальные кнопки `Показать файлы`/`Скрыть файлы` остались размером 32x32; скрытого toggle и tiny-buttons нет; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/logs: общий скан 17 маршрутов на `.76`                                                                                                                                                                                                                    | OK        | `/dashboard`, `/chat/new`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground`: подозрительных tiny/dead/OAuth/английских кнопок не найдено; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA API/Playwright: центр обновления на `.76`                                                                                                                                                                                                                            | OK        | `/api/update/status` возвращает HTTP 200: Workspace `2.3.0-comandos.76`, latest `2.3.0-comandos.76`, `updateAvailable=false`, `state=current`; Hermes Agent тоже `current`. `/dashboard` не показывает лишнюю карточку обновления; browser console/page errors/failed requests/HTTP 4xx/5xx пустые |
| QA Playwright/API/logs: MCP disabled-state на `.76`                                                                                                                                                                                                                     | OK        | `/api/gateway-status` возвращает `mcp=false` и `mcpFallback=false`; `/api/mcp` отдаёт управляемый `capability_unavailable` с русским текстом; `/mcp` показывает объяснение про Hermes Agent gateway и не показывает активные кнопки `Добавить сервер`/`Установить`/`Настроить`/`Открыть логи`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/logs: OpenAI Codex CLI wizard на `.76`                                                                                                                                                                                                                    | OK        | Повторно проверен сценарий `Добавить провайдера` -> `OpenAI Codex` -> `CLI-вход`: `Открыть терминал` открывает `/terminal`, отдельный клик `Перейти к проверке` показывает русскую подсказку про `~/.codex/auth.json`, `Готово` закрывает мастер; старые `Start OAuth`, `OAuth device flow not supported` и требование Codex API key не появляются; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/logs: `Новая сессия` на `.76`                                                                                                                                                                                                                            | OK        | Из `/chat/main` клик по `Новая сессия` переводит на `/chat/new`, показывает `Новый чат`, стартовые подсказки и composer `Напишите задачу...`; browser console/page errors/HTTP 4xx/5xx пустые, единственный `net::ERR_ABORTED` у `/api/events` ожидаем при SPA-переходе, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/components/terminal/terminal-panel.tsx src/components/terminal/terminal-workspace.tsx src/components/terminal/debug-panel.tsx src/components/terminal/debug-panel.test.tsx src/routes/api/debug-analyze.ts` после `.77`                                                                                         | OK        | Терминальные компоненты и route диагностики проходят lint; осталось только старое предупреждение ESLint про `.eslintignore` |
| `pnpm exec vitest run src/server/comandos-version-lock.test.ts src/components/terminal/debug-panel.test.tsx src/routes/api/-terminal-session-lifecycle.test.ts` после `.77`                                                                                            | OK        | 8 тестов прошли; проверены version-lock, русские loading/failure сообщения панели диагностики и stale terminal API |
| `pnpm build` после `.77`                                                                                                                                                                                                                                                | OK        | Production build прошёл; остались старые предупреждения Vite про размер чанков и mixed dynamic/static imports |
| QA manual update: `.76 -> .77`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён через `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; package.json показывает `2.3.0-comandos.77`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: терминальные кнопки на `.77`                                                                                                                                                                                                                        | OK        | `/terminal` открылся после входа; `Новая вкладка терминала` создала `Терминал 2`; `AI-анализ терминала` открыл `Диагностика терминала` с русским результатом `Кратко` и без старых `Analyzing...`/`Debug analysis failed`; `Закрыть панель диагностики` убрала панель; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/screens/chat/components/message-actions-bar.tsx`, `pnpm exec vitest run src/server/comandos-version-lock.test.ts`, `pnpm build` после `.78`                                                                                                        | OK        | Кнопка копирования сообщения проходит lint; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA Playwright/logs: `/operations` и общий скан 17 маршрутов на `.78`                                                                                                                                                                                                     | FIXED     | На `.77` общий скан нашёл пять видимых кнопок 26x26 без имени в истории чата агента на `/operations`. В `.78` `MessageActionsBar` даёт кнопке русские `aria-label`/`title` `Копировать сообщение`; `/operations` больше не имеет подозрительных кнопок; общий скан `/dashboard`, `/chat/new`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground` чистый; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/screens/agents/components/operations-new-agent-modal.tsx src/screens/agents/components/operations-settings-modal.tsx`, version-lock, `pnpm build` после `.79`                                                                                      | OK        | Модалки создания агента и настроек операций проходят lint; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA manual update: `.78 -> .79`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён через `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; package.json показывает `2.3.0-comandos.79`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: модалки `/operations` на `.79`                                                                                                                                                                                                                       | FIXED     | Playwright-скан нашёл у `.78` две видимые 36x36 кнопки закрытия без имени: `Новый агент` и `Настройки операций`. В `.79` обе получили русские `aria-label`/`title`; полный проход `/operations` открыл `Результаты`, `Обзор`, `Настройки`, `Новый агент`, `Добавить агента`, создал временный `qa-ops-*`, открыл настройки, сохранил и удалил его. Browser console/page errors/failed requests/HTTP 4xx/5xx пустые, но проход выявил английские toast-ы `Agent created` и `Agent settings saved`, поэтому это вынесено в `.80` |
| `pnpm exec eslint src/screens/agents/hooks/use-operations.ts`, version-lock, `pnpm build` после `.80`                                                                                                                                                                    | OK        | Hook операций проходит lint; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA manual update: `.79 -> .80`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён через `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; package.json показывает `2.3.0-comandos.80`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: полный цикл `/operations` на `.80`                                                                                                                                                                                                                   | FIXED     | На `.79` после действий были английские toast-ы `Agent created` и `Agent settings saved`. В `.80` русифицированы успешные и ошибочные сообщения создания/сохранения/удаления агента, `Custom`, `No activity yet`, `Next`, `Last`, `soon`. Повторный проход нажал `Результаты`, `Обзор`, `Настройки`, `Новый агент`, `Добавить агента`, `Создать агента`, `Сохранить`, `Удалить агента`; временный `qa-ops-*` удалён, видимых кнопок 19, подозрительных кнопок нет, русские toast-ы `Настройки операций сохранены`, `Агент создан`, `Настройки агента сохранены`, `Агент удалён` показаны, английских snippets нет, browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/routes/settings/index.tsx`, version-lock, `pnpm build` после `.81`                                                                                                                                                                                | OK        | Настройки проходят lint; все `Switch` в `src/routes/settings/index.tsx` имеют `aria-label` или `aria-labelledby`; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA manual update: `.80 -> .81`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён через `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; package.json показывает `2.3.0-comandos.81`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: переключатели настроек на `.81`                                                                                                                                                                                                                     | FIXED     | На `.80` в разделах настроек `Модель и провайдер`, `Голос` и `Экран` были видимые `role=switch` без доступного имени. В `.81` добавлены русские `aria-label` для `Память включена`, `Профиль пользователя`, `Включить умный выбор`, `Включить STT`, `Потоковый вывод`, `Показывать рассуждения`, `Показывать стоимость`, `Компактный вид`. Живой проход нажал и восстановил 16 переключателей в `claude`, `voice`, `display`, `chat`, `notifications`; безымянных, не нажавшихся и не восстановленных переключателей нет; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| QA Playwright/logs: `/dashboard` на `.81`                                                                                                                                                                                                                               | OK        | Видимых действий 36, пустых/крошечных подозрительных кнопок и ссылок нет. Живой проход проверил `Настроить панель`/`Завершить настройку панели`, `Новый чат`, `Терминал`, `Навыки`, `Настройки`, карточку сессии `Открыть чат`, плавающую кнопку `Открыть чат` и `Закрыть панель чата`; все действия открывают нужный экран или возвращают состояние назад; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/screens/chat/components/chat-header.tsx`, version-lock, `pnpm build` после `.82`                                                                                                                                                                  | OK        | Шапка чата проходит lint; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA manual update: `.81 -> .82`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён через `/opt/comandos/hermes-qa/install/comandos-update.sh workspace`; package.json показывает `2.3.0-comandos.82`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: статус чата `/chat/new` и `/chat/main` на `.82`                                                                                                                                                                                                      | FIXED     | На `.81` обычный online-индикатор `На связи` был видимой кнопкой 8x8 без полезного действия. В `.82` при нормальном статусе он стал `span role="status"` с русским именем `На связи`; кнопкой остаётся только stale-состояние `Нужно обновить`. Живой проход `/chat/new` и `/chat/main` подтвердил: tiny-кнопок и кнопок `На связи` нет, `role=status` есть на обоих маршрутах; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/screens/chat/components/chat-composer.tsx`, version-lock, `pnpm build` после `.83`                                                                                                                                                                  | OK        | Действия composer и mobile-menu русифицированы; version-lock согласован; production build прошёл со старыми Vite-предупреждениями про размер чанков и mixed dynamic/static imports |
| QA manual update: `.82 -> .83`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён штатным update-скриптом; package.json показывает `2.3.0-comandos.83`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: карта действий чата на `.83`                                                                                                                                                                                                                         | PARTIAL   | `/chat/new` и `/chat/main` не показывали видимых английских строк и подозрительных кнопок после русификации composer. При этом аудит доступных имён нашёл скрытые английские/пустые места в шапке чата: переключение сессии и переименование держались только на `title`, поиск сессий не имел явного русского имени, индикатор мышления отдавал `Thinking: Adaptive`; исправлено в `.84` |
| `pnpm exec eslint src/screens/chat/components/chat-header.tsx`, version-lock, `pnpm build` после `.84`                                                                                                                                                                    | OK        | Шапка чата проходит lint; добавлены русские доступные имена для переключения сессии, переименования, поиска сессий и индикатора мышления; version-lock согласован; production build прошёл |
| QA manual update: `.83 -> .84`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён штатным update-скриптом; package.json показывает `2.3.0-comandos.84`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: первая отправка из `/chat/new` на `.84`                                                                                                                                                                                                               | PARTIAL   | Ответ модели появлялся в UI, но network фиксировал `POST /api/send-stream :: net::ERR_ABORTED`. Причина: `/chat/new` запускал stream и сразу переходил на `/chat/main`, а затем стабилизация ключа сессии могла отменить тот же активный stream. Первая часть исправлена в `.85` через pending-send на целевом маршруте, финальная отмена активного stream исправлена в `.86` |
| `pnpm exec vitest run src/server/comandos-version-lock.test.ts`, `pnpm build` после `.85` и `.86`                                                                                                                                                                        | OK        | Version-lock согласован; production build прошёл. Затронутая логика собрана и проверена живым сценарием |
| QA manual update: `.85 -> .86`                                                                                                                                                                                                                                          | OK        | QA-контур обновлён штатным update-скриптом; package.json показывает `2.3.0-comandos.86`; `comandos-workspace.service` и `hermes-gateway.service` active |
| QA Playwright/logs: отправка и основные кнопки чата на `.86`                                                                                                                                                                                                              | FIXED     | Фокусный тест отправки показал `sendCount=1`, `sendFinished=1`, `sendFailures=[]`: `/api/send-stream` завершается без abort. Полный проход нажал `Настройки чата`, `Копировать сообщение`, `Переключить сессию`, `Открыть панель агента`, `Переключить панель инспектора`, `Скрыть панель агента`, `Новая сессия`, `Удалить вложение`, `Голосовой ввод`, `Отправить сообщение`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежие workspace/gateway логи без ошибок |
| `pnpm exec eslint src/screens/chat/chat-screen.tsx src/hooks/use-chat-stream.ts`, version-lock, `pnpm build` после `.87`                                                                                                                                                  | OK        | Большой `chat-screen.tsx` и legacy `use-chat-stream` проходят целевой lint без ошибок; убраны мёртвые условия, лишние optional-chain и shadow warnings без изменения проверенного сценария отправки |
| QA update: `.87/.88`                                                                                                                                                                                                                                                     | PARTIAL   | `.87` частично легла на диск, но installed-state остался `.86`: старый update-скрипт падал на `/tmp/comandos-workspace-node-check.log` и диск был забит backup-ами. `.88` исправила приватный node-check log и прореживание backup-ов, но выявила ошибку шаблона `${VAR:-{{VAR}}}`: Bash добавлял лишнюю `}` и сборка уходила в `workspace}}` |
| QA update/live contour: `.89`                                                                                                                                                                                                                                            | FIXED     | `.89` исправила подстановку путей update-скрипта и проверку плейсхолдеров. Найдена реальная схема QA: `qa.194.113.38.109.nip.io` идёт через Caddy на `127.0.0.1:3130` и user-service `hermesqa`, а не на `clawd`/`3030`. Обновлены оба контура: `/opt/comandos/hermes-qa/workspace` и реально обслуживаемый `/opt/comandos/hermes/workspace`; старые тяжёлые `workspace-update-*` backup-и прорежены, диск восстановлен с ~90% до ~38% |
| QA Playwright/logs: чистая отправка чата на `.89`                                                                                                                                                                                                                        | OK        | После перезапуска `hermesqa` service HTML больше не отдаёт старые asset-ссылки. Чистый Playwright-прогон: вход, `/chat/new`, отправка сообщения; `sendCount=1`, `sendFinished=1`, `sendFailures=[]`, browser console/page errors/failed requests/HTTP 4xx/5xx пустые. Свежие логи `hermesqa` без ошибок; есть только диагностический portable fallback `missing=[sessions, enhancedChat, mcp, mcpFallback, dashboard]` |
| `pnpm exec eslint src/components/agent-view/agent-view-panel.tsx`, version-lock, `pnpm build` после `.90`                                                                                                                                                                  | OK        | Большой `agent-view-panel.tsx` проходит целевой lint без ошибок; сохранена защита от отменённых async-запросов, убраны старые optional-chain/dead-condition/shadow замечания и несуществующие eslint-disable |
| QA/main update restart: `.91/.92`                                                                                                                                                                                                                                          | FIXED     | `.91` добавила перезапуск `comandos-workspace.service` от имени владельца сервиса, но живой QA-прогон показал слабое место: workspace после root-обновления принадлежит `root`, поэтому одного `stat "$REMOTE_WORKSPACE_DIR"` мало. `.92` выбирает пользователя по `REMOTE_SERVICE_USER`, `REMOTE_HERMES_HOME`, user-systemd файлу или владельцу workspace. QA-лог показал `Перезапускаю панель пользователя hermesqa через 5 сек`, основной контур — `Перезапускаю панель пользователя clawd через 5 сек` |
| QA/main update/live contour: `.92`                                                                                                                                                                                                                                         | OK        | QA и основной контур обновлены до `2.3.0-comandos.92`. `qa.194.113.38.109.nip.io/chat/new` и `194.113.38.109.nip.io/chat/new` отвечают HTTP 200; `comandos-workspace.service` и `hermes-gateway.service` active; `node --check dist/server/server.js` чистый; installed-state обоих контуров показывает `.92`; диск около 38% |
| QA Playwright/logs: чистая отправка чата на `.92`                                                                                                                                                                                                                          | OK        | Чистый браузерный прогон после обновления: экран входа с паролем, `/chat/new`, ожидание composer, отправка через Enter; итог `/chat/main`, `textareaCount=2`, `sendCount=1`, `sendFinished=1`, browser console/page errors/failed requests/HTTP 4xx/5xx пустые. Свежие логи `hermesqa` без ошибок, только диагностический portable fallback `source=gateway-capabilities mode=portable` |
| QA Playwright/logs: OpenAI Codex CLI wizard на `.92`                                                                                                                                                                                                                        | OK        | Проверен реальный путь `/settings/providers`: `Добавить провайдера` -> `OpenAI Codex` -> `CLI-вход` -> `Перейти к проверке`. В мастере один Codex-провайдер, доступен CLI-вход, виден `codex login`, старые `Start OAuth` и `OAuth device flow not supported` отсутствуют; browser console/page errors/failed requests/HTTP 4xx/5xx пустые; свежий journal `hermesqa` без записей за время проверки. Добавлен regression-тест `provider-wizard.test.ts` на этот путь |
| QA Playwright/logs: индикатор окна контекста на `.93`                                                                                                                                                                                                                       | FIXED     | Общий скан `.92` нашёл в `/operations` видимую пустую ссылку `PreviewCardTrigger` у полосы окна контекста. В `.93` trigger в `ContextBar` и `ContextMeter` стал кнопкой с русским доступным именем `Окно контекста: N% использовано`; старые английские подписи индикатора переведены. QA `.93`: пустых видимых ссылок/кнопок нет, trigger виден как `button`, browser console/page errors/HTTP 4xx/5xx пустые, свежие логи `hermesqa` без ошибок |
| QA Playwright/logs: общий скан 17 маршрутов на `.93`                                                                                                                                                                                                                        | OK        | `/chat/new`, `/dashboard`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground`: 681 видимое действие; подозрительных пустых/tiny/OAuth/английских/fatal элементов нет, browser console/page errors и HTTP 4xx/5xx пустые. `ERR_ABORTED` были только на ресурсах при быстрых переходах; свежий journal `hermesqa` без ошибок |
| QA Playwright/logs: mobile/desktop контекстная полоса на `.94`                                                                                                                                                                                                              | FIXED     | После `.93` в мобильной ветке `ContextBar` оставалась старая английская `aria-label`. В `.94` она переведена. QA обновлена до `2.3.0-comandos.94`: desktop `/operations` и mobile `/chat/main` показывают `Окно контекста: 0% использовано`; старого `Context`/`% used` нет; browser console/network и свежий journal `hermesqa` без ошибок |
| QA Playwright/logs: `/memory` -> `База знаний` на `.95/.96`                                                                                                                                                                                                      | FIXED     | На `.95` русифицированы пустые состояния и API-ошибки базы знаний. На `.96` исправлен откат клика `GitHub-репозиторий`: проверены `Настройки`, `GitHub-репозиторий`, `Локальная папка`, `Граф связей`; старые `Graph view`, `Knowledge Pages`, `No knowledge base found`, `Repository`, `Save`, `Knowledge graph` не появляются; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, journal свежий без ошибок |
| QA Playwright/logs: свёрнутое меню и верхняя вкладка терминала на `.97/.98`                                                                                                                                                                                                  | FIXED     | На `.97` пункты свёрнутого меню и кнопка пользователя получили русские `aria-label/title`. На `.98` верхняя вкладка терминала получила имя `Вкладка терминала: Терминал 1`, а её закрытие — `Закрыть вкладку терминала: Терминал 1`. QA `.98`: installed-state и package показывают `2.3.0-comandos.98`, `/dashboard` отвечает 200; после входа, клика `Свернуть меню` и сбора всех видимых `button/a/role=button` в левой и верхней зоне `empty=[]`, `required=[]`, `Открыть меню` раскрывает меню обратно; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежий journal без ошибок |
| QA Playwright/logs: общий `Switch` и широкий скан на `.99`                                                                                                                                                                                                                  | FIXED     | На `.98` общий скан нашёл два 1x1 checkbox без имени на `/settings`. В `.99` скрытый `input[aria-hidden="true"]` в общем `Switch` больше не принимает pointer-events. QA `.99`: installed-state и package показывают `2.3.0-comandos.99`; `/settings?section=chat` показывает 6 switch, `Показывать сообщения инструментов` переключается `false -> true -> false`; empty/tiny пустые; широкий скан 17 маршрутов прошёл 795 controls без empty/tiny/badText, browser console/network пустые, journal без ошибок |
| QA Playwright/logs: навыки, switch, чат и рой на `.109/.110/.111/.112`                                                                                                                                                                                                      | FIXED     | `.109` русифицировала карточки навыков, `.110` убрала внутренний `input on` общего Switch из аудита, `.111` исправила `Local Chat`, высоту контекстной полосы и часть подписей роя, `.112` закрыла оставшиеся `KM Agent` и категории `models`/`skills`/`evaluation`. Локально прошли целевой eslint, version-lock test, `git diff --check`, `pnpm build`, `node --check dist/server/server.js`; QA обновлена до `.112`. Широкий скан 17 маршрутов не дал console/page/network/HTTP 5xx ошибок; единственный шум — не кнопка, а `tabpanel` `/skills` с именами навыков/авторов. Точный скан реальных `button/a/input/select/textarea/role=button/switch` вернул `[]`; свежие журналы `comandos-workspace`/`hermes-gateway` без ошибок |
| QA Playwright/logs: ручные клики `OpenAI Codex` и `Новая сессия` на `.112`                                                                                                                                                                                                  | OK        | `/settings/providers`: `Добавить провайдера` -> `OpenAI Codex` -> `CLI-вход` показывает `codex login`, терминальную подсказку и не показывает старые `Start OAuth`, `OAuth device flow not supported`, `Key required`, `Key set`; browser console/page/network/HTTP 5xx пустые. `/chat/main` -> `Новая сессия` переводит на `/chat/new`, composer виден, `Local Chat` не появляется; отменённые `ERR_ABORTED` на `/api/events` и `/api/gateway/sessions` связаны с переходом страницы, HTTP 5xx нет; свежие журналы `comandos-workspace`/`hermes-gateway` без ошибок |
| QA Playwright/API/logs: вход, лицензия, обновления и настройки на `.112`                                                                                                                                                                                                     | OK        | Чистый контекст `/chat/new`: пустой пароль держит `Продолжить` disabled, неверный пароль показывает `Неверный пароль.`, правильный пароль открывает панель; browser console/page/network/HTTP 5xx пустые. `/api/auth-check` вернул `licenseRequired=true`, `licenseActivated=true`, статус лицензии `active`; `/api/license-check` вернул `active` без английской ошибки. `/api/update/status` вернул Workspace и Hermes Agent как `managed/current`, `updateAvailable=false`, `pendingReleaseNotes=0`; версия Workspace `2.3.0-comandos.112`, Hermes Agent `v0.14.0`. Разделы `/settings?section=connection|claude|agent|voice|display|appearance|chat|notifications|language` открылись, активный пункт найден, контент есть, подозрительных кнопок и старых английских технических сообщений нет; свежие журналы `comandos-workspace`/`hermes-gateway` без ошибок |
| QA/main Playwright/API/logs: настройки и декоративная desktop-подложка на `.114`                                                                                                                                                                                              | FIXED     | `.114` поставлена штатными `/opt/comandos/hermes-qa/install/comandos-update.sh workspace` и `/opt/comandos/hermes/install/comandos-update.sh workspace`; на QA и main package/state показывают `2.3.0-comandos.114`, `dist/server` без пустых JS, `comandos-workspace.service` и `hermes-gateway.service` active. Чистые Playwright-прогоны подтвердили: `Пользователь -> Настройки` и нижняя шестерёнка открывают `/settings`; `/settings?section=connection|claude|agent|voice|display|appearance|chat|notifications|language` открываются, `Показать запасную модель` раскрывается, `/api/update/status` отдаёт Workspace и Agent `current`, подозрительной кнопки `Свернуть боковое меню` больше нет; browser console/page/failed requests/HTTP 4xx/5xx пустые, свежие журналы без ошибок. Временный `qa-button-audit-*.md` удалён из `/home/hermesqa/workspace` |
| Main Playwright/logs: `OpenAI Codex` wizard на `.114`                                                                                                                                                                                                                         | OK        | На основном домене `/settings/providers`: `Добавить провайдера` -> видимая карточка `OpenAI Codex ... CLI-вход` -> `CLI-вход` -> `Перейти к проверке`. Мастер показывает `codex login`, старые `Start OAuth`, `OAuth device flow not supported`, `Key required`, `Key set` не появляются; browser console/page/failed requests/HTTP 4xx/5xx пустые, свежие журналы без новых ошибок |
| QA/main Playwright/API/logs: широкий скан 17 маршрутов на `.115`                                                                                                                                                                                                                | FIXED     | `.115` поставлена штатными `/opt/comandos/hermes-qa/install/comandos-update.sh workspace` и `/opt/comandos/hermes/install/comandos-update.sh workspace`; на QA и main package/state показывают `2.3.0-comandos.115`, `dist/server` без пустых JS, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-проход по `/chat/new`, `/dashboard`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground` подтвердил: старые `Start OAuth`, `OAuth device flow not supported`, `Key required`, `Key set`, `Свернуть боковое меню` не видны; `unnamedNumberInputs=[]`; `/api/update/status` возвращает Workspace `current`. Browser page/HTTP 5xx ошибок нет; `ERR_ABORTED` связан с переходом между страницами и закрытием долгих `/api/events`, `/api/context-usage`, `/api/terminal-stream`; свежие workspace/gateway журналы без ошибок |
| QA/main Playwright/logs: xterm helper textarea на `.116`                                                                                                                                                                                                                        | FIXED     | `.116` поставлена штатными update-скриптами на QA и основной контур; на обоих package и cached manifest показывают `2.3.0-comandos.116`, `dist/client` без `server*.js`, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright вошёл через `/api/auth`, открыл `/terminal`, дождался `.xterm-helper-textarea` и подтвердил `ariaLabel/title=Ввод терминала`; `textarea[aria-label="Terminal input"]` не найден, видимый текст `Terminal input` отсутствует. Browser page/HTTP 5xx ошибок нет; свежие workspace/gateway журналы без warning/error/failed/exception |
| QA/main Playwright/logs: вторичный слой controls на `.117`                                                                                                                                                                                                                       | FIXED     | `.117` поставлена штатными update-скриптами на QA и основной контур; на обоих package и cached manifest показывают `2.3.0-comandos.117`, `dist/client` без `server*.js`, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-прогон прошёл `/chat/new`, `/dashboard`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground` на обоих доменах: старые строки `Attach image`, `Mobile navigation`, `Undo last message`, `Memory Files`, `Analytics period`, `Previous`, `Next`, `Approvals`, `Agent options`, `View Output`, `Kill session`, `Send directive` в видимых controls не найдены; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway журналы без warning/error/failed/exception |
| QA/main Playwright/logs: скрытый слой overview/run console/dashboard на `.118`                                                                                                                                                                                                   | FIXED     | `.118` поставлена штатными update-скриптами на QA и основной контур; на обоих package и cached manifest показывают `2.3.0-comandos.118`, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-прогон прошёл `/chat/new`, `/chat/main`, `/dashboard`, `/files`, `/terminal`, `/jobs`, `/tasks`, `/conductor`, `/operations`, `/swarm`, `/swarm2`, `/memory`, `/skills`, `/mcp`, `/profiles`, `/settings`, `/settings/providers`, `/playground` на обоих доменах: старые строки `Configure`, `View Output`, `Send directive`, `Stream`, `Timeline`, `Artifacts`, `Usage peaked`, `Cache reads`, `No active mission`, `Mission Status`, `Approval Required`, `Key Findings`, `Total Tokens`, `No role description` не найдены; browser console/page errors/HTTP 4xx/5xx пустые, свежие workspace/gateway журналы без warning/error/failed/exception |
| QA/main Playwright/logs: title страницы провайдеров на `.119`                                                                                                                                                                                                                    | FIXED     | `.119` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.119`, `comandos-workspace.service` и `hermes-gateway.service` active. Фокусный Playwright-прогон `/settings/providers` на QA и main подтвердил title `Провайдеры моделей — COMANDOS AI Workspace`; старые `Provider Setup` и `Настраивайте providers` не найдены, browser console/network пустые, свежие workspace/gateway журналы без warning/error/failed/exception |
| QA/main Playwright/API/logs: HermesWorld reservation на `.121`                                                                                                                                                                                                                    | FIXED     | `.121` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.121`, `comandos-workspace.service` и `hermes-gateway.service` active. `GET /api/hermesworld/reservations` на QA/main возвращает `200`, `ok=true`, `enabled=false`, `reason=storage_not_configured`; `/reserve` показывает disabled-кнопку `Бронирование пока не подключено`; короткий проход `/chat/new`, `/conductor`, `/operations`, `/swarm2`, `/dashboard`, `/reserve`, `/reserve/confirm`, `/missing-button-audit` прошёл без browser console errors, HTTP 4xx/5xx и старых английских строк; свежие workspace/gateway журналы без warning/error/failed/exception |
| QA/main Playwright/logs: заголовки групп сайдбара на `.122`                                                                                                                                                                                                                       | FIXED     | `.122` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.122`, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright вошёл на QA/main, открыл `/chat/new`, `/chat/main`, `/dashboard` и подтвердил, что `Основное`/`Знания` больше не являются `button/a/role=button/link`; отдельные кнопки `Свернуть Основное` и `Свернуть Знания` нажимаются и возвращаются через `Развернуть ...`; browser console errors, HTTP 4xx/5xx и свежие workspace/gateway ошибки отсутствуют |
| QA/main Playwright/logs: голосовые настройки на `.123`                                                                                                                                                                                                                             | FIXED     | `.123` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.123`, `comandos-workspace.service` и `hermes-gateway.service` active. `/settings?section=voice` на QA/main показывает `Локально (Whisper)`, `Базовая (base)` и остальные русские варианты размера модели; старые `Local (Whisper)`, сырой список `tiny/base/small/medium/large`, `Включить STT`, `Провайдер STT` и placeholder `auto` не найдены в проверенных controls; browser console/page/HTTP 4xx/5xx и свежие workspace/gateway журналы чистые |
| QA/main Playwright/logs: Agora на `.124`                                                                                                                                                                                                                                              | FIXED     | `.124` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.124`, `comandos-workspace.service` и `hermes-gateway.service` active. `/agora` на QA/main показывает `Агора`, `локальная площадь агентов`, `{n} онлайн`, `Участники`, `Чат комнаты`; Playwright нажал собственный профиль, статус, профиль участника, `Поприветствовать`, поле `Сообщение в комнату` и кнопку отправки. Старые строки `the first AI agent community`, `Your profile`, `Say something to the room`, `Your Profile`, `Status`, `Bio`, `Avatar`, `Online`, `WASD or arrow keys`, `The Agora`, `Send`, `Wave` не найдены в видимом тексте; browser console/page/HTTP 5xx и свежие workspace/gateway журналы чистые |
| QA/main Playwright/logs: Monaco textarea на `.125`                                                                                                                                                                                                                                      | FIXED     | `.125` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.125`, `comandos-workspace.service` и `hermes-gateway.service` active. `/files` на QA/main содержит один видимый `textarea.ime-text-area` с `ariaLabel/title=Редактор файлов`; пустых видимых textarea нет, `/memory` не показывает открытый Monaco textarea в текущем состоянии, browser console/page/HTTP 5xx и свежие workspace/gateway журналы чистые |
| QA/main Playwright/API/logs: ошибки профилей на `.126`                                                                                                                                                                                                                                  | FIXED     | `.126` поставлена штатными update-скриптами на QA и основной контур; package и cached manifest показывают `2.3.0-comandos.126`, `comandos-workspace.service` и `hermes-gateway.service` active. На QA и main временный профиль создан через API, активирован, прямой delete активного профиля вернул `200 ok:false` и `Активный профиль нельзя удалить.`, затем `default` восстановлен, временный профиль удалён, хвостов нет. На QA дополнительно нажаты UI-кнопки `/profiles`: `Создать профиль`, `Детали`/`Обновить`, `Переименовать`, `Активировать`, возврат `default`, `Удалить`; browser console/HTTP 5xx и свежие workspace/gateway журналы чистые |
| QA Playwright/logs: composer и быстрые кнопки чата на `.112`                                                                                                                                                                                                                | OK        | `/chat/new`: пустой composer показывает видимый `Голосовой ввод` и disabled `Отправить сообщение`; `Добавить файл` открывает file chooser. Быстрые кнопки `Аудит рабочей папки`, `Записать правило в память`, `Создать файл` заполняют именно видимый composer textarea; первый пустой textarea в DOM — скрытый helper xterm, не поле чата. `Настройки чата` открывает компактные действия; `Рабочая папка: workspace`, `Усилие рассуждения: Низкое` и `deepseek-chat` открываются без fatal/undefined/traceback и без browser console/page/HTTP 5xx; свежие журналы `comandos-workspace`/`hermes-gateway` без ошибок |
| QA Playwright/API/logs: `/files` ручной файловый цикл на `.112`                                                                                                                                                                                                             | OK        | Проверены `Новый файл`, `Сохранить`, открытие файла из списка, редактирование в preview, повторное `Сохранить`, контекстное меню, `Скачать` и удаление через API-проверку. Временный `qa-button-audit-*.md` создан, сохранён, скачан с правильным именем и удалён; финальный `find` на QA не нашёл тестовых файлов. Browser console/page errors/failed requests/HTTP 4xx/5xx пустые; свежие журналы `comandos-workspace`/`hermes-gateway` без ошибок |
| QA Playwright/logs: `/terminal` ручной цикл вкладки, диагностики и ввода на `.112`                                                                                                                                                                                          | OK        | `/terminal` показывает видимый xterm; `Новая вкладка терминала` создаёт `Терминал 2`; `AI-анализ терминала` открывает русскую диагностику без старых `Analyzing`, `Debug analysis failed`, `Failed to analyze`; безопасная команда `echo qaterminalok` уходит через 18 POST на `/api/terminal-input`, все ответы `200`, вывод виден в терминале. Browser console/page errors/failed requests/HTTP 4xx/5xx пустые; свежие журналы без ошибок, только штатный portable fallback `source=gateway-capabilities mode=portable` |
| QA Playwright/logs: `/conductor` ручной цикл на `.112/.113`                                                                                                                                                                                                                | FIXED     | На `.112` ручной проход `Как это работает`, `Новая миссия`, `Открыть настройки оркестратора`, `Выбрать`, `Изменить схему офиса` выявил HTTP 500 на `/api/files?path=~` при выборе папки проекта. В `.113` выбор папки больше не вызывает защищённый `/api/files` для `~`, `~/...` и абсолютных путей, показывает русское объяснение `Этот путь можно выбрать вручную...`, а поля `Описание миссии`, `Папка проектов`, `Максимум агентов одновременно`, `Режим с подтверждением` получили явные имена. QA обновлена штатным update-скриптом до `.113`; повторный Playwright-прогон подтвердил: help открывается, форма миссии disabled на пустом вводе и enabled после текста, настройки редактируются и восстанавливаются, выбор папки открывается без UI/server ошибки, схема офиса показывает `Сетка`/`Круглый стол`/`Штаб`; browser console/page errors/failed requests/HTTP 4xx/5xx пустые, свежий journal без ошибок |
| QA Playwright/logs: `/swarm` и `/swarm2` ручной цикл роя на `.113`                                                                                                                                                                                                         | OK        | `/swarm` и `/swarm2` оба ведут на текущий `Swarm2Screen`; карта нашла 169 видимых действий на каждом маршруте, подозрительных пустых/tiny/OAuth/английских `Local Chat`/`KM Agent` элементов нет. Ручной проход нажал `Как это работает` с закрытием, `Уведомления роя`, `Добавить агента` без сохранения, вкладки `Доска`/`Входящие`/`Работа`/`Управление`, `МАРШРУТ`, `Настройки оркестратора`, режимы `авто`/`один агент`/`всем`, проверил disabled/enabled `Направить задачу` без отправки, фильтры `Все`/`В работе`/`Проверка`/`Стоп`/`Готово`, `Активный рой`/`Офис`, настройки типового агента, `Следующая панель`/`Предыдущая панель`, `Добавить задачу`, `Открыть терминал`, `Открыть чат`. Browser console/page errors/HTTP 4xx/5xx пустые; `ERR_ABORTED` были только на отменённых долгих запросах при переходах; свежий journal без ошибок, только штатный portable fallback `source=gateway-capabilities mode=portable` |
| Live Telegram router: основной `comandos-telegram.service`                                                                                                                                                                                                                 | OK        | На основном контуре найден активный user service `comandos-telegram.service` с отдельным `/opt/comandos/hermes/telegram/.env`; QA-контур Telegram не содержит. Живой прогон через локальную userbot-сессию к `@comandos_hermes_bot`: `/start`, `/buttons` и короткий обычный запрос получили ответы; inline-разметка не пришла ни в одном ответе, `button_protocol_enabled=false`, `demo_menu_enabled=false`, `state.json` и callback-состояние не созданы. Свежий journal показывает обычный запуск Hermes CLI для текстового запроса с `rc=0`; ошибок и утечек кнопок нет. Локальный отчёт сохранён вне репозитория: `Telegram/reports/hermes-router-button-audit-20260606-182244` |
| Live Telegram voice/STT: основной `comandos-telegram.service`                                                                                                                                                                                                              | OK        | Сгенерирован короткий русский Telegram voice note, отправлен в `@comandos_hermes_bot` через локальную userbot-сессию; ответ получен, inline-разметка не пришла. Свежий journal подтвердил реальный путь: загрузка `faster-whisper` model `base`, распознавание `language=ru`, затем Hermes CLI завершился с `rc=0`; `state.json` и callback-состояние не созданы. Локальный отчёт сохранён вне репозитория: `Telegram/reports/hermes-router-voice-audit-20260606-152558` |
| `pnpm exec vitest run src/server/comandos-version-lock.test.ts`, `pnpm build` после `.113`                                                                                                                                                                                | OK        | Version-lock подтвердил синхрон `package.json`, `update-manifest.json` и `comandos-hermes.lock`; production build прошёл локально и на QA update. Целевой `pnpm exec eslint src/screens/gateway/conductor.tsx` пока не является чистым gate для этой правки: файл содержит 40 старых lint-ошибок до затронутого участка (`import/order`, `Array<T>`, старые optional-chain), их нужно закрывать отдельным механическим проходом |
| Инструкции чистой установки: `AGENTS.md`                                                                                                                                                                                                                                 | FIXED     | `AGENTS.md` всё ещё требовал обязательные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_USER_ID`, хотя env/deploy/README уже разрешают установку панели без Telegram. Инструкция исправлена: Telegram необязателен, но если включён, нужны оба поля; добавлен явный fallback на заполненный `comandos-hermes.env.example`; финальный отчёт допускает `Telegram bot: active или disabled` |
| Shell-проверка env для чистой установки                                                                                                                                                                                                                                  | OK        | Временный env без Telegram прошёл `scripts/check-config.sh`: `Telegram: disabled`, модель выбрана `deepseek/deepseek-chat`, `DOMAIN` уходит на `IP.nip.io`; env с одним `TELEGRAM_BOT_TOKEN` без `TELEGRAM_USER_ID` честно падает `Telegram requires both...`; `bash -n` для `check-config.sh`, `deploy.sh`, `install.sh`, `setup.sh` чистый |
| Fresh GitHub install smoke                                                                                                                                                                                                                                                | OK        | В пустой временной папке выполнен `curl -fsSL https://raw.githubusercontent.com/Comandosai/comandos-deploy-hub/main/stacks/hermes/install.sh | bash`: появились `comandos-hermes.env`, `comandos-hermes.env.example`, `AGENTS.md`, `deploy.sh`, `workspace`; свежий `AGENTS.md` содержит fallback на example и `Telegram bot: active или disabled`; env содержит только простые VPS-поля `ROOT_IP`, `ROOT_USER`, `SSH_KEY_PATH`, `ROOT_PASSWORD`; `HERMES_INLINE_BUTTONS_ENABLED=false`; пустой env падает только по SSH-доступу и ключу модели |
| Поиск секретов в diff                                                                                                                                                                                                                                           | OK        | API-ключи, Telegram-токены, пароли, private key не найдены                                                                                                                                                                                                                                                                                                                                                                                                          |

## Что ещё требует живой коробочной проверки

| Область                                       | Почему ещё TODO                                                                                                                                                                                                                      |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Полный MCP runtime                            | Перепроверено 2026-06-07 на QA и main: `/api/connection-status` отдаёт `mcp=false`, `mcpFallback=false`; кнопка/экран уже проверены как disabled. Полный цикл добавления MCP-сервера надо повторить, когда Hermes Agent реально даст MCP runtime |
| Обновление Hermes Agent при новой версии      | Перепроверено 2026-06-07 на QA и main: `/api/update/status` показывает Workspace `.130` в состоянии `current`, Hermes Agent `0.14.0` в состоянии `current`, `updateAvailable=false`, `pendingReleaseNotes=0`. Кнопку обновления Agent надо повторить, когда manifest будет указывать более новую проверенную версию агента |

Проверено отдельно: inline-кнопки Telegram заложены, но по умолчанию выключены. В `comandos-hermes.env.example` стоит `HERMES_INLINE_BUTTONS_ENABLED=false`, `deploy.sh` пишет этот флаг с дефолтом `false`, `router.py` дефолтит `button_protocol_enabled=False`, unit-тест `test_buttons_are_disabled_by_default` проходит. На текущем main-конфиге VPS `button_protocol_enabled=False`; QA Telegram не установлен. Read-only проверка 2026-06-06 подтвердила: на QA нет `comandos-telegram.service` и нет Telegram env, а на основном контуре есть активный user service `comandos-telegram.service`. Живой Telegram smoke 2026-06-06 через `@comandos_hermes_bot` подтвердил: `/start`, `/buttons`, обычный текст и голосовое сообщение отвечают, inline-кнопки не появляются, callback-state не создаётся, STT распознаёт voice как `language=ru`, Hermes CLI завершает текстовый и voice-запросы с `rc=0`.

## Текущий вывод

Локальная и живая тестовая панель стали существенно ближе к коробочному состоянию: основные разделы открываются, мёртвые кнопки из найденного прохода исправлены или честно отключены, ложный OAuth для Codex убран, пользовательские ошибки лицензии и gateway русифицированы, ложный `OpenAI` в панели агента и кривые версии обновлений исправлены. Первый чат на живом VPS с моделью теперь проверен: сообщение отправляется, ответ модели появляется в UI, история доступна через `/api/history`. Чистая установка из GitHub на отдельный QA-контур `.36` завершилась успешно и не перезаписала основной live-домен. На `.37` дополнительно исправлен ранний запуск рабочих API на экране пароля/лицензии, на `.38` — чистый управляемый отказ кнопки активации лицензии, на `.41` — автозагрузка внешнего HermesWorld iframe заменена на понятный русский экран запуска, на `.43` — мастер провайдеров больше не предлагает Codex API-ключ/OAuth и ведёт только через CLI-вход, на `.44` — MCP-раздел проверен на QA и не показывает активных действий при отсутствии MCP runtime, на `.48` — файловое контекстное меню проверено настоящим правым кликом и удаление файла работает без console/network/systemd ошибок, на `.49` — кнопка обновления Workspace проверена настоящим кликом на QA и больше не должна показывать пользователю сырой браузерный или backend-текст при рестарте/обрыве, на `.50` — кнопка `Сохранить` в провайдерах больше не ломает DeepSeek, заменяя его на `custom`, на `.51` — меню сессии больше не показывает английский `Pin session`, на `.52` — старый сохранённый ростер роя больше не выводит английские миссии агентов, а профильные кнопки на `/profiles` прошли полный цикл create/details/save/rename/activate/delete с проверкой API и логов. На `.53` — кнопка `Сохранить` в задачах больше не теряет приоритет, теги и срок через kanban backend; весь цикл `/tasks` проверен живыми кликами и серверными логами. На `.60` — весь цикл `/jobs` снова проверен живыми кликами: создание, редактирование, пауза, возобновление, ручной запуск, история, поиск и удаление работают без browser/network/server ошибок. На `.61` — QA update-status больше не падает в `unknown/unsupported`: managed-установка без `.git` читает cached manifest и показывает `managed/current`. На `.69` — экран входа с реальным паролем и активной лицензией проверен на QA: неверный пароль теперь показывает русское управляемое сообщение без HTTP-ошибки, а правильный вход открывает рабочую панель. На `.70` — кнопка `Голосовой ввод` больше не молчит в браузере, где диктовка не стартует: пользователь видит русскую ошибку, а сеть, консоль и логи остаются чистыми. На `.71` — закрытая правая панель больше не отдаёт скрытые кнопки инспектора; кнопки и вкладки правой панели проверены живыми кликами, сеть, консоль и серверные журналы чистые. На `.73` — закрытая файловая панель больше не раскладывает невидимые кнопки поверх чата; файловый цикл `Обновить`/`Новый файл`/`Загрузить`/`Скачать`/`Удалить` проверен на QA живыми кликами, временные файлы удалены, сеть и журналы чистые. На `.73` дополнительно пройдены вкладки настроек `Модель и провайдер`, `Поведение агента`, `Голос`, `Экран`, `Тема`, `Чат`, `Сигналы` и `Язык`; тестовые серверные и браузерные настройки восстановлены, сеть и журналы чистые. На `.75` убраны ложные кнопки агентов в офисе оркестратора: элементы без действия больше не выглядят кнопками, а выбор схемы офиса остался рабочим. На `.76` убран старый скрытый 1x1 toggle файловой панели: в дереве кнопок остались только реальные видимые `Показать файлы`/`Скрыть файлы`; быстрый общий скан 17 маршрутов не нашёл новых подозрительных tiny/dead/OAuth/английских кнопок; центр обновления показывает Workspace и Hermes Agent в состоянии `current`; MCP-раздел повторно проверен и честно показывает disabled-состояние без активных кнопок установки при `mcp=false`; мастер OpenAI Codex повторно ведёт через `codex login` и терминал, без ложного OAuth; `Новая сессия` переводит из существующего чата в `/chat/new` и показывает пустой новый чат. На `.77` терминальная диагностика больше не показывает английские fallback-сообщения; `Новая вкладка терминала`, `AI-анализ терминала` и закрытие панели диагностики проверены на живой QA-панели с пустыми browser console/network и свежими workspace/gateway логами. На `.78` исправлены безымянные кнопки копирования сообщений в `/operations`; повторный общий скан 17 маршрутов не нашёл подозрительных кнопок, fatal-текста, browser/network ошибок или свежих systemd-ошибок. Инструкции для агента теперь совпадают с установщиком: Telegram не требуется для первого запуска, а заполненный `comandos-hermes.env.example` можно использовать вместо пустого основного env. Fresh GitHub install smoke подтвердил, что новые инструкции и env-шаблон уже реально скачиваются из `main`.

Дополнение 2026-06-06: на `.79` модалки `/operations` получили русские имена для кнопок закрытия `Новый агент` и `Настройки операций`. На `.80` полный живой цикл `/operations` повторён после исправления английских toast-ов: `Настройки`, `Новый агент`, `Добавить агента`, создание временного `qa-ops-*`, сохранение и удаление агента проходят с русскими сообщениями и без browser/network/systemd ошибок. На `.81` разделы настроек повторно проверены по переключателям: видимых безымянных `role=switch` больше нет, 16 переключателей нажимаются и возвращаются в исходное состояние, свежие browser/systemd ошибки не появились. На `.81` также проверен `/dashboard`: главные действия верхней панели, карточки сессии и плавающей чат-панели работают без браузерных и серверных ошибок. На `.82` обычный online-статус `На связи` в шапке чата больше не является крошечной кнопкой: `/chat/new` и `/chat/main` показывают его как `role=status`, а браузер и серверные логи после проверки чистые. На `.83` русифицированы действия composer и меню чата, но аудит нашёл доступные имена шапки, которые ещё были слабыми; на `.84` они исправлены. На `.84` первая отправка из `/chat/new` выявила браузерный abort у `/api/send-stream`; на `.86` причина закрыта: stream стартует на целевом маршруте и не отменяется при стабилизации той же сессии. Полный проход основных кнопок чата на `.86` завершился без browser/network/systemd ошибок.

Позднее 2026-06-06: на `.87` закрыт целевой lint `chat-screen.tsx`/`use-chat-stream`, на `.90` закрыт целевой lint большого `agent-view-panel.tsx`. При обновлении обнаружена и исправлена критичная проблема deploy-пути: QA-домен обслуживается `hermesqa` на `3130`, а не `clawd` на `3030`; дефект `.88` с лишней `}` в путях исправлен в `.89`. На `.91/.92` закрыта ещё одна реальная проблема обновлялки: после root-обновления workspace может принадлежать `root`, поэтому update-скрипт теперь определяет пользователя user-service по `REMOTE_HERMES_HOME` или systemd-файлу и перезапускает именно `hermesqa`/`clawd`. После `.92` оба домена `/chat/new` отвечают HTTP 200, оба installed-state показывают `.92`, чистая отправка показывает `sendCount=1`, `sendFinished=1`, без browser/network/server ошибок. Диск очищен от старых тяжёлых служебных backup-ов и держится примерно на 38% занятости.

Ещё позже 2026-06-06: на `.93` закрыта пустая ссылка в индикаторе окна контекста. Теперь это кнопка с русским именем для клавиатуры и автопроверок; живой `/operations` на QA не показывает пустых видимых действий, а свежие браузерные и серверные журналы чистые. Повторный общий скан 17 маршрутов на `.93` прошёл 681 видимое действие без подозрительных пустых/tiny/OAuth/английских/fatal элементов; свежий journal `hermesqa` без ошибок. На `.94` отдельно закрыта мобильная подпись этой же полосы: desktop и mobile QA показывают русское имя `Окно контекста: 0% использовано`. На `.95/.96` отдельно пройдена `Память -> База знаний`: русифицированы пустые состояния, граф, поля метаданных и API-ошибки; исправлен откат клика `GitHub-репозиторий`; QA `.96` показывает поля `Репозиторий`, `Ветка`, `Подпапка`, возвращается на `Локальная папка`, открывает `Граф связей` и не даёт browser/network/server ошибок. На `.97/.98` отдельно закрыты безымянные controls свёрнутого меню и верхней вкладки терминала: QA `.98` не нашёл пустых имён в левой/верхней зоне, меню раскрывается обратно, браузерные ошибки и свежие серверные ошибки отсутствуют. На `.99` закрыты скрытые 1x1 checkbox от Base UI Switch на `/settings`; видимый switch проверен кликом туда-обратно, а широкий скан 17 маршрутов прошёл 795 controls без empty/tiny/badText и без browser/network/server ошибок. На `.100/.101/.102/.103/.104/.105` закрыт большой пласт пользовательского английского в HermesWorld: игровые кнопки, диалоги, карта, чат, журнал, инвентарь, хранилище основателя, настройки, online-индикатор, админ-панель, мобильные действия, terminal mobile input, стартовая карточка, HUD цели, вывески, помещения, подписи NPC, зоны и базовые реплики получили русские подписи; важный дефект `.100` тоже исправлен — `Запустить в панели` теперь монтирует локальный проверяемый `PlaygroundScreen`, а внешний `hermes-world.ai` оставлен только как отдельная ссылка; runtime-крэш `.103` на NPC устранён; HEAD-проба отсутствующих GLB отключена, чтобы не давать 404 в браузере. На `.106/.107/.108` живой проход HermesWorld закрыл реальные дефекты кликабельности: правая панель больше не перекрывает rail `Инвентарь`/`Журнал заданий`/`Карта`/`Настройки`/`Режим фокуса`, нижний utility dock больше не перекрывается инвентарём, а кнопки снимка, полноэкранного режима и копирования ссылки получили русские имена. QA `.108` прожал все перечисленные кнопки, браузерная консоль, сетевые ошибки и свежие логи `comandos-workspace`/`hermes-gateway` чистые.

На `.109/.110/.111/.112` продолжен проход по живым разделам вне HermesWorld: русифицированы карточки и категории навыков, скрыт внутренний input общего Switch, `Local Chat` заменён на `Локальный чат`, `KM Agent` — на `Агент знаний`, а контекстная полоса получила нормальную кликабельную высоту. После обновления QA до `.112` широкий скан 17 маршрутов не нашёл браузерных, сетевых или серверных ошибок; точный скан реальных кнопок, ссылок, полей и переключателей вернул пустой список подозрений. Единственный шум широкого скана — большая область `tabpanel` на `/skills`, где встречаются имена навыков и авторов; это не кликабельная кнопка и не рабочий дефект.

На `.114` отдельно закрыта desktop-подложка настроек: она больше не является невидимой кнопкой `Свернуть боковое меню`. QA и основной контур обновлены штатными update-скриптами, версии package/state синхронизированы, user-service active. Живые Playwright-прогоны подтвердили вход в настройки через меню пользователя и нижнюю шестерёнку, открытие всех 9 разделов настроек, работу `Показать запасную модель`, `update/status=current` для Workspace и Agent, отсутствие browser console/page/network/server ошибок и отсутствие подозрительной overlay-кнопки. На основном домене дополнительно повторно проверен проблемный путь `OpenAI Codex`: мастер ведёт через `CLI-вход` и `codex login`, без ложного OAuth.

На `.115` закрыты слабые доступные имена числовых полей настроек. QA и основной контур обновлены до `2.3.0-comandos.115`, package/state совпадают, `dist/server` без пустых JS, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-проход 17 маршрутов на обоих доменах подтвердил: старые OAuth-тексты и ложные `Key required`/`Key set` не видны, декоративной кнопки `Свернуть боковое меню` нет, неподписанных `input type=number` нет, `/api/update/status` показывает Workspace `current`. Свежие systemd-журналы без ошибок; `ERR_ABORTED` в браузере связан с обычным переходом между страницами и закрытием долгих запросов.

На `.116` закрыто последнее найденное английское имя внутри терминала: служебный textarea xterm больше не отдаёт `Terminal input`, а называется `Ввод терминала`. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.116`, `dist/client` без `server*.js`, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright на обоих доменах вошёл через `/api/auth`, открыл `/terminal`, подтвердил `ariaLabel/title=Ввод терминала`, отсутствие `textarea[aria-label="Terminal input"]`, отсутствие видимого текста `Terminal input` и отсутствие browser page/HTTP 5xx ошибок. Свежие журналы `comandos-workspace` и `hermes-gateway` на QA/main без warning/error/failed/exception.

На `.117` закрыт следующий слой английских подписей в дополнительных controls: вложения, мобильная навигация, undo/clear чата, usage meter, файлы памяти, период аналитики, календарь, approvals и панели агентов. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.117`, `dist/client` без `server*.js`, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-прогон 17 маршрутов на обоих доменах прошёл без browser console/page errors, HTTP 4xx/5xx и без старых строк `Attach image`, `Mobile navigation`, `Undo last message`, `Memory Files`, `Analytics period`, `Previous`, `Next`, `Approvals`, `Agent options`, `View Output`, `Kill session`, `Send directive` в видимых controls. Свежие журналы `comandos-workspace` и `hermes-gateway` на QA/main без warning/error/failed/exception. Статический поиск после этого показал следующий слой для отдельного прохода: `View Output` в `overview-tab.tsx` и `Send directive...` в `run-console.tsx`.

На `.118` закрыт найденный после `.117` скрытый слой: обзор статуса миссии, карточки агентов, консоль запуска, блоки подтверждения, отчёт запуска, аналитические подсказки панели и onboarding-подсказка монитора расхода переведены на русский. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.118`, `comandos-workspace.service` и `hermes-gateway.service` active. Широкий Playwright-прогон 18 маршрутов на обоих доменах прошёл без browser console/page errors, HTTP 4xx/5xx и без старых строк `Configure`, `View Output`, `Send directive`, `Stream`, `Timeline`, `Artifacts`, `Usage peaked`, `Cache reads`, `No active mission`, `Mission Status`, `Approval Required`, `Key Findings`, `Total Tokens`, `No role description` в видимом тексте. Свежие журналы `comandos-workspace` и `hermes-gateway` на QA/main без warning/error/failed/exception.

На `.119` исправлен новый дефект, найденный живым проходом `.118`: `/settings/providers` работал, но title страницы оставался английским, а onboarding-подсказка смешивала русский текст с английским словом `providers`. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.119`, `comandos-workspace.service` и `hermes-gateway.service` active. Фокусный Playwright-прогон `/settings/providers` на обоих доменах подтвердил title `Провайдеры моделей — COMANDOS AI Workspace`, отсутствие старых строк, отсутствие browser console/network ошибок и чистые свежие журналы workspace/gateway.

На `.121` исправлен дефект, найденный живым проходом `.120`: страница `/reserve` обращалась к `GET /api/hermesworld/reservations`, но на QA/main не были настроены Supabase/Resend, поэтому браузер видел технический `500`, а пользователь видел форму как рабочую. Теперь API счётчика при неподключённом хранилище возвращает управляемый `200` с `enabled=false`, форма до настройки сервиса отключена и показывает `Бронирование пока не подключено`. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.121`, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright/API подтвердили: `/reserve`, `/reserve/confirm`, `/chat/new`, `/conductor`, `/operations`, `/swarm2`, `/dashboard` и 404-страница открываются без browser console errors, HTTP 4xx/5xx и старых английских строк; свежие журналы workspace/gateway чистые.

На `.122` закрыт дефект, найденный широким live-сканом `.121`: заголовки групп сайдбара `Основное` и `Знания` выглядели как обычные подписи, но были маленькими ссылками на последний посещённый раздел. Теперь это не действия, а текстовые заголовки; сворачивание и разворачивание осталось отдельными понятными кнопками. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.122`, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright подтвердил на `/chat/new`, `/chat/main` и `/dashboard`: `Основное`/`Знания` больше не попадают в список кнопок/ссылок, `Свернуть Основное` и `Свернуть Знания` нажимаются, browser console/network и свежие журналы workspace/gateway чистые.

На `.123` закрыт дефект, найденный широким live-сканом `.122`: в настройках голоса распознавание речи выглядело частично как внутренняя техническая настройка, а не как готовая русская панель. `Local (Whisper)`, `tiny/base/small/medium/large`, `Включить STT`, `Провайдер STT` и placeholder `auto` заменены на русские видимые подписи и доступные имена, при этом реальные значения конфига не изменены. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.123`, сервисы active. Playwright подтвердил `/settings?section=voice` на обоих доменах, старые строки отсутствуют, browser console/network и свежие журналы workspace/gateway чистые.

На `.124` закрыт дефект, найденный широким live-сканом `.123`: маршрут `/agora` выглядел как английская демо-заготовка. Шапка, title, счётчик online, профиль, статусы, аватары, список участников, чат, кнопка приветствия, подсказка движения и mock-реплики переведены на русский. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.124`, сервисы active. Playwright подтвердил на обоих доменах: собственный профиль открывается, статус нажимается, профиль участника открывается, `Поприветствовать` добавляет сообщение, поле чата отправляет текст, старые английские строки отсутствуют, browser console/page/HTTP 5xx и свежие журналы workspace/gateway чистые.

На `.125` закрыт дефект, найденный широким live-сканом `.124`: на `/files` внутренний textarea Monaco был видимым и без имени. Теперь общий helper подписывает такие поля, файловый редактор называется `Редактор файлов`, редактор памяти — `Редактор памяти`. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.125`, сервисы active. Playwright подтвердил на обоих доменах: `/files` содержит видимый Monaco textarea с `ariaLabel/title=Редактор файлов`, пустых видимых textarea нет; `/memory` в текущем состоянии не открывает Monaco textarea; browser console/page/HTTP 5xx и свежие журналы workspace/gateway чистые.

На `.126` закрыт дефект, найденный ручным сценарием `/profiles` на `.125`: ожидаемые ошибки управления профилями могли уходить как технический HTTP 500. Теперь активный профиль, отсутствующий профиль, дубль имени, неверное имя и запрет на изменение `default` возвращают `ok:false` с русским сообщением. QA и основной контур обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.126`, сервисы active. Playwright/API подтвердили на обоих доменах: временный профиль создаётся, активируется, delete активного профиля возвращает `Активный профиль нельзя удалить.`, `default` восстанавливается, временный профиль удаляется без хвостов. На QA дополнительно прожаты UI-кнопки профилей: создание, детали/обновление, переименование, активация, возврат `default` и удаление; browser console/HTTP 5xx и свежие журналы workspace/gateway чистые.

На `.127/.128` закрыт ручной сценарий MCP при текущем portable-gateway без MCP runtime. На `.126` маршрут `/mcp` показывал общую заглушку серверной части, хотя API уже возвращал управляемое состояние `capability_unavailable`. Теперь полный экран MCP открывается на QA и основном контуре, вкладки `Установленные` и `Каталог` являются настоящими tabs, поиск и фильтр имеют русские имена, `Добавить сервер` и `Установить` честно отключены с русской причиной, а `Источники -> Добавить источник` открывает форму с доступными полями `ID источника`, `Название источника`, `URL источника`, `Доверие источника`, `Формат источника`, `Источник включён`. QA и main обновлены до `2.3.0-comandos.128`, package и cached manifest совпадают, `comandos-workspace.service` и `hermes-gateway.service` active. Playwright подтвердил на обоих доменах: `/api/gateway-status` возвращает `mcp=false`, `mcpFallback=false`, `/api/mcp` возвращает HTTP 200 `capability_unavailable`, старые `built-in`/`generic-json` в источниках не видны, ошибки пустой формы русские, browser console/page/HTTP 5xx и свежие журналы workspace/gateway чистые.

На `.129` закрыт остаточный дефект в настройках голоса: размеры локальной модели распознавания больше не показывают пользователю технические значения `tiny`, `base`, `small`, `medium`, `large` в скобках. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.129`, `comandos-workspace.service` и `hermes-gateway.service` active. Внешний TLS с локальной машины во время проверки временно закрывал соединение, поэтому Playwright был сначала запущен через SSH-туннель к тому же Caddy; с самого сервера оба домена отдавали HTTP 200. После восстановления внешнего HTTPS Playwright повторён напрямую на QA и main: `/settings?section=voice` содержит опции `Очень маленькая`, `Базовая`, `Малая`, `Средняя`, `Большая`, без технических слов в видимых подписях; browser console/page/HTTP 5xx и свежие журналы workspace/gateway чистые.

На `.130` закрыты повторяющиеся маленькие зоны клика, найденные широким live-сканом `.129`. Кнопки `Свернуть Основное` и `Свернуть Знания` в сайдбаре теперь имеют стабильные `32x32`, а кнопки `Настройки агента ...` в `/swarm` и `/swarm2` больше не сжимаются внутри карточек. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.130`, user-сервисы `comandos-workspace.service` и `hermes-gateway.service` active. Playwright подтвердил на обоих доменах: `/dashboard` и `/chat/new` сворачивают и разворачивают группы, `/swarm` и `/swarm2` открывают настройки агента, размеры проверенных кнопок минимум `32x32`; browser console/page/HTTP 5xx и свежие user-журналы чистые.

На `.131` закрыт следующий дефект, найденный широким live-сканом `.130`: в `/tasks` пять настоящих кнопок добавления задачи в колонку были `18x18`. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.131`, user-сервисы active. Playwright подтвердил на обоих доменах: `Добавить в "Разобрать"`, `Добавить в "Готово к работе"`, `Добавить в "В работе"`, `Добавить в "Проверка"` и `Добавить в "Стоп"` имеют `32x32`, клик открывает форму создания задачи, browser console/page/HTTP 5xx и свежие user-журналы чистые.

На `.132` закрыт следующий слой маленьких кнопок в рое: режимы маршрутизации `авто`, `один агент`, `всем` и фильтры статуса агентов `ВСЕ`, `В РАБОТЕ`, `ПРОВЕРКА`, `СТОП`, `ГОТОВО` на `/swarm` и `/swarm2`. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.132`, user-сервисы active. Playwright на обоих доменах кликнул все режимы и фильтры; размеры стали `49-94x32`, browser console/page/HTTP 5xx и свежие user-журналы чистые.

На `.133` закрыта нижняя кнопка `Настройки` в общем сайдбаре: широкий скан `.132` показывал `28x28` на большинстве маршрутов. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.133`, user-сервисы active. Playwright на обоих доменах кликнул нижнюю `Настройки` на `/chat/new`, `/dashboard`, `/files`, `/tasks`, `/settings`, `/playground`; везде размер `32x32`, клик открывает `/settings`, browser console/page/HTTP 5xx и свежие user-журналы чистые.

На `.134` закрыты верхние маленькие кнопки `/tasks` и `/jobs`, найденные широким live-сканом `.133`: `Показать готовые` было `121x26`, `Обновить` и `Обновить список` были `28x28`, `Новая задача` была `120x28`. QA и main обновлены штатными update-скриптами, package и cached manifest показывают `2.3.0-comandos.134`, user-сервисы active. Playwright на обоих доменах кликнул `/tasks`: `Показать готовые`, `Скрыть готовые`, `Обновить`, `Новая задача`; форма задачи открылась, размеры стали `115-125x32`, `32x32`, `120x32`. На `/jobs` кликнул `Обновить список`, размер стал `32x32`. Icon-only действия карточек заданий тоже увеличены в коде до `32x32`, но на текущих QA/main карточек заданий не было видно для живого клика. Browser console/page/HTTP 5xx и свежие user-журналы чистые.

Следующий обязательный шаг перед продажей и видео: продолжить ручные сценарии, где автоскан не заменяет человека. Из явно зависимых от окружения сценариев сейчас остались только полный MCP runtime, если Hermes Agent начнёт отдавать `mcp=true`, и проверка обновления Hermes Agent, когда manifest покажет новую версию; 2026-06-07 оба условия на QA и main ещё не наступили.
