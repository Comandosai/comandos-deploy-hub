# 🚀 n8n PRO: Автоматическая установка с PostgreSQL & Python

Этот скрипт предназначен для быстрой и надежной установки **n8n** на чистый VPS под управлением Ubuntu/Debian. 

> [!IMPORTANT]
> Это не просто n8n. Это оптимизированная сборка с базой данных PostgreSQL, поддержкой Python 3 и полным набором системных шрифтов.

## ✨ Особенности
- **PostgreSQL 16:** Стабильность при высоких нагрузках (забудьте про ошибки SQLite).
- **Python 3 & Pip:** Возможность выполнять Python-скрипты прямо в workflow.
- **Шрифты:** Включает Noto Sans, FontAwesome и другие для корректной генерации PDF и изображений.
- **Traefik + SSL:** Автоматическое получение SSL-сертификатов от Let's Encrypt.
- **Worker Mode:** Архитектура с воркером и Redis для параллельного выполнения задач.

## 🛠 Требования
- Чистый сервер с **Ubuntu 20.04/22.04/24.04**.
- Минимум **2 ГБ ОЗУ** (рекомендуется 4 ГБ).
- Домен, направленный на IP вашего сервера (A-запись).

## 🚀 Быстрый старт

Просто выполните эту команду на вашем сервере:

```bash
curl -sSL https://raw.githubusercontent.com/Comandosai/comandos-deploy-hub/main/n8n-stack/setup.sh | sudo bash
```

Или склонируйте весь хаб:

```bash
git clone https://github.com/Comandosai/comandos-deploy-hub.git
cd comandos-deploy-hub/n8n-stack
sudo ./setup.sh
```

## 📋 Управление
Скрипт установки также служит инструментом управления:
- `sudo ./install.sh --stop` — остановить n8n.
- `sudo ./install.sh --start` — запустить n8n.
- `sudo ./install.sh --logs` — просмотр логов.

## 📁 Структура папок
После установки все данные будут находиться в папке `n8n-docker-pro`:
- `n8n_data/` — настройки и workflow.
- `postgres_data/` — база данных.
- `output/` — папка для выходных файлов (доступна внутри n8n как `/data/output`).

## 🛡 Безопасность
- Скрипт автоматически генерирует уникальные ключи шифрования и пароли для БД.
- Доступ к портам БД и Redis закрыт снаружи, всё общение идет внутри Docker-сети.

## 📄 Лицензия
MIT
